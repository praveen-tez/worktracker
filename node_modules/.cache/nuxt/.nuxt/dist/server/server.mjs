import { defineProdDiagnostics } from "nostics";
import { ansiFormatter } from "nostics/formatters/ansi";
import { computed, createApp, createVNode, defineAsyncComponent, defineComponent, effectScope, getCurrentInstance, getCurrentScope, h, hasInjectionContext, inject, isReactive, isReadonly, isRef, isShallow, mergeProps, onErrorCaptured, onServerPrefetch, provide, reactive, ref, resolveDynamicComponent, shallowReactive, toRaw, toRef, unref, useSSRContext } from "vue";
import { createHooks } from "C:/Users/user/Downloads/Projects/Test Mine/Tracker - logs/node_modules/nuxt/node_modules/hookable/dist/index.mjs";
import { getContext } from "C:/Users/user/Downloads/Projects/Test Mine/Tracker - logs/node_modules/nuxt/node_modules/unctx/dist/index.mjs";
import { createError, sanitizeStatusCode } from "C:/Users/user/Downloads/Projects/Test Mine/Tracker - logs/node_modules/@nuxt/nitro-server/dist/h3.mjs";
import { decodePath, encodePath, hasProtocol, isEqual, isScriptProtocol, joinURL, parseQuery, parseURL, stringifyParsedURL, stringifyQuery, withQuery } from "C:/Users/user/Downloads/Projects/Test Mine/Tracker - logs/node_modules/ufo/dist/index.mjs";
import { $fetch } from "C:/Users/user/Downloads/Projects/Test Mine/Tracker - logs/node_modules/ofetch/dist/node.mjs";
import { baseURL } from "#internal/nuxt/paths";
import { defu } from "C:/Users/user/Downloads/Projects/Test Mine/Tracker - logs/node_modules/defu/dist/defu.mjs";
import { headSymbol, useHead } from "C:/Users/user/Downloads/Projects/Test Mine/Tracker - logs/node_modules/@unhead/vue/dist/index.mjs";
import { ssrIncludeBooleanAttr, ssrInterpolate, ssrLooseContain, ssrLooseEqual, ssrRenderAttr, ssrRenderAttrs, ssrRenderClass, ssrRenderComponent, ssrRenderDynamicModel, ssrRenderList, ssrRenderStyle, ssrRenderSuspense, ssrRenderVNode } from "vue/server-renderer";
//#region node_modules/nuxt/dist/app/diagnostics/_shared.js
/**
* Shared configuration for the runtime (E<N>xxx) diagnostics catalogs.
*
* Catalogs are split by domain and imported directly where used (no barrel),
* so the browser bundle only pulls in the codes a module references. Pair the
* pure-call annotations on each `defineDiagnostics()` with dev-guarded,
* statement-level report calls so report-only diagnostics strip from production.
*
* Codes are stable, fully-qualified `NUXT_E<NNNN>` identifiers. Codes with a
* dedicated docs page resolve a `see:` URL via {@link docsBase}; the rest opt
* out with `docs: false`.
*/
function docsBase(code) {
	return `https://nuxt.com/docs/4.x/errors/${code.replace("NUXT_", "").toLowerCase()}`;
}
var ansi = (open, close) => (s) => `\x1B[${open}m${s}\x1B[${close}m`;
var colors = {
	red: ansi(31, 39),
	yellow: ansi(33, 39),
	cyan: ansi(36, 39),
	gray: ansi(90, 39),
	bold: ansi(1, 22),
	dim: ansi(2, 22)
};
ansiFormatter(colors);
var prodReporter = (diagnostic) => {
	console.error(`[${diagnostic.name}]`);
};
var prodReporters = [prodReporter];
//#endregion
//#region node_modules/nuxt/dist/app/diagnostics/core.js
/**
* E1xxx
* Core / Nuxt-instance / lifecycle runtime diagnostics.
*/
var appDiagnostics = /* #__PURE__ */ defineProdDiagnostics({
	docsBase,
	reporters: prodReporters
});
//#endregion
//#region virtual:nuxt:node_modules%2F.cache%2Fnuxt%2F.nuxt%2Fnuxt.config.mjs
var nuxtLinkDefaults = {
	"componentName": "NuxtLink",
	"prefetch": true,
	"prefetchOn": { "visibility": true }
};
//#endregion
//#region node_modules/nuxt/dist/app/nuxt.js
function getNuxtAppCtx(id = "nuxt-app") {
	return getContext(id, { asyncContext: false });
}
var NuxtPluginIndicator = "__nuxt_plugin";
/** @since 3.0.0 */
function createNuxtApp(options) {
	let hydratingCount = 0;
	const nuxtApp = {
		_id: options.id || "nuxt-app",
		_scope: effectScope(),
		provide: void 0,
		versions: {
			get nuxt() {
				return "4.5.2";
			},
			get vue() {
				return nuxtApp.vueApp.version;
			}
		},
		payload: shallowReactive({
			...options.ssrContext?.payload || {},
			data: shallowReactive({}),
			state: reactive({}),
			once: /* @__PURE__ */ new Set(),
			_errors: shallowReactive({})
		}),
		static: { data: {} },
		runWithContext(fn) {
			if (nuxtApp._scope.active && !getCurrentScope()) return nuxtApp._scope.run(() => callWithNuxt(nuxtApp, fn));
			return callWithNuxt(nuxtApp, fn);
		},
		isHydrating: false,
		deferHydration() {
			if (!nuxtApp.isHydrating) return () => {};
			hydratingCount++;
			let called = false;
			return () => {
				if (called) return;
				called = true;
				hydratingCount--;
				if (hydratingCount === 0) {
					nuxtApp.isHydrating = false;
					return nuxtApp.callHook("app:suspense:resolve");
				}
			};
		},
		_asyncDataPromises: {},
		_asyncData: shallowReactive({}),
		_state: shallowReactive({}),
		_payloadRevivers: {},
		...options
	};
	nuxtApp.payload.serverRendered = true;
	if (nuxtApp.ssrContext) {
		nuxtApp.payload.path = nuxtApp.ssrContext.url;
		nuxtApp.ssrContext.nuxt = nuxtApp;
		nuxtApp.ssrContext.payload = nuxtApp.payload;
		nuxtApp.ssrContext.config = {
			public: nuxtApp.ssrContext.runtimeConfig.public,
			app: nuxtApp.ssrContext.runtimeConfig.app
		};
	}
	nuxtApp.hooks = createHooks();
	nuxtApp.hook = nuxtApp.hooks.hook;
	{
		const contextCaller = async function(hooks, args) {
			for (const hook of hooks) await nuxtApp.runWithContext(() => hook(...args));
		};
		nuxtApp.hooks.callHook = (name, ...args) => nuxtApp.hooks.callHookWith(contextCaller, name, args);
	}
	nuxtApp.callHook = nuxtApp.hooks.callHook;
	nuxtApp.provide = (name, value) => {
		const $name = "$" + name;
		defineGetter(nuxtApp, $name, value);
		defineGetter(nuxtApp.vueApp.config.globalProperties, $name, value);
	};
	defineGetter(nuxtApp.vueApp, "$nuxt", nuxtApp);
	defineGetter(nuxtApp.vueApp.config.globalProperties, "$nuxt", nuxtApp);
	const runtimeConfig = options.ssrContext.runtimeConfig;
	nuxtApp.provide("config", runtimeConfig);
	return nuxtApp;
}
/** @since 3.0.0 */
async function applyPlugin(nuxtApp, plugin) {
	if (typeof plugin === "function") {
		const run = () => nuxtApp.runWithContext(() => plugin(nuxtApp));
		const { provide } = await run() || {};
		if (provide && typeof provide === "object") for (const key in provide) nuxtApp.provide(key, provide[key]);
	}
}
/** @since 3.0.0 */
async function applyPlugins(nuxtApp, plugins) {
	let error;
	for (const plugin of plugins) try {
		await applyPlugin(nuxtApp, plugin);
	} catch (e) {
		if (!nuxtApp.payload.error) throw e;
		error ||= e;
	}
	if (error) throw nuxtApp.payload.error || error;
}
/** @since 3.0.0 */
/* @__NO_SIDE_EFFECTS__ */
function defineNuxtPlugin(plugin) {
	if (typeof plugin === "function") return plugin;
	const _name = plugin._name || plugin.name;
	delete plugin.name;
	return Object.assign(plugin.setup || (() => {}), plugin, {
		[NuxtPluginIndicator]: true,
		_name
	});
}
/**
* Ensures that the setup function passed in has access to the Nuxt instance via `useNuxtApp`.
* @param nuxt A Nuxt instance
* @param setup The function to call
* @since 3.0.0
*/
function callWithNuxt(nuxt, setup, args) {
	const fn = () => args ? setup(...args) : setup();
	const nuxtAppCtx = getNuxtAppCtx(nuxt._id);
	return nuxt.vueApp.runWithContext(() => nuxtAppCtx.callAsync(nuxt, fn));
}
function tryUseNuxtApp(id) {
	let nuxtAppInstance;
	if (hasInjectionContext()) nuxtAppInstance = getCurrentInstance()?.appContext.app.$nuxt;
	nuxtAppInstance ||= getNuxtAppCtx(id).tryUse();
	return nuxtAppInstance || null;
}
function useNuxtApp(id) {
	const nuxtAppInstance = tryUseNuxtApp(id);
	if (!nuxtAppInstance) throw appDiagnostics.NUXT_E1001();
	return nuxtAppInstance;
}
/** @since 3.0.0 */
/* @__NO_SIDE_EFFECTS__ */
function useRuntimeConfig(_event) {
	return useNuxtApp().$config;
}
function defineGetter(obj, key, val) {
	Object.defineProperty(obj, key, { get: () => val });
}
//#endregion
//#region node_modules/nuxt/dist/app/utils.js
import.meta.url.replace(/\/app\/.*$/, "/");
//#endregion
//#region node_modules/nuxt/dist/app/components/injections.js
var PageRouteSymbol = Symbol("route");
//#endregion
//#region node_modules/nuxt/dist/app/diagnostics/navigation.js
/**
* E2xxx
* Navigation / routing / middleware runtime diagnostics.
*/
var navigationDiagnostics = /* #__PURE__ */ defineProdDiagnostics({
	docsBase,
	reporters: prodReporters
});
//#endregion
//#region node_modules/nuxt/dist/app/composables/router.js
/** @since 3.0.0 */
var useRouter = () => {
	return useNuxtApp()?.$router;
};
/**
* Whether the current effect scope is (a descendant of) the component instance's scope.
* A detached scope (e.g. `createSharedComposable`) outlives the component, so the
* per-page route injected there would freeze after navigation (#18903).
*/
function isScopeWithinInstance(instance) {
	const instanceScope = instance.scope;
	let scope = getCurrentScope();
	while (scope) {
		if (scope === instanceScope) return true;
		scope = scope.parent;
	}
	return false;
}
/** @since 3.0.0 */
var useRoute = (() => {
	if (hasInjectionContext()) {
		const instance = getCurrentInstance();
		if (!instance || isScopeWithinInstance(instance)) return inject(PageRouteSymbol, useNuxtApp()._route);
	}
	return useNuxtApp()._route;
});
/** @since 3.0.0 */
/* @__NO_SIDE_EFFECTS__ */
function defineNuxtRouteMiddleware(middleware) {
	return middleware;
}
/** @since 3.0.0 */
var isProcessingMiddleware = () => {
	try {
		if (useNuxtApp()._processingMiddleware) return true;
	} catch {
		return false;
	}
	return false;
};
var HTML_ATTR_UNSAFE_RE = /[&"'<>]/g;
var HTML_ATTR_ENCODE_MAP = {
	"&": "&amp;",
	"\"": "&quot;",
	"'": "&#x27;",
	"<": "&lt;",
	">": "&gt;"
};
function encodeForHtmlAttr(value) {
	return value.replace(HTML_ATTR_UNSAFE_RE, (c) => HTML_ATTR_ENCODE_MAP[c]);
}
/**
* A helper that aids in programmatic navigation within your Nuxt application.
*
* Can be called on the server and on the client, within pages, route middleware, plugins, and more.
* @param {RouteLocationRaw | undefined | null} [to] - The route to navigate to. Accepts a route object, string path, `undefined`, or `null`. Defaults to '/'.
* @param {NavigateToOptions} [options] - Optional customization for controlling the behavior of the navigation.
* @returns {Promise<void | NavigationFailure | false> | false | void | RouteLocationRaw} The navigation result, which varies depending on context and options.
* @see https://nuxt.com/docs/4.x/api/utils/navigate-to
* @since 3.0.0
*/
var navigateTo = (to, options) => {
	to ||= "/";
	const toPath = typeof to === "string" ? to : "path" in to ? resolveRouteObject(to) : useRouter().resolve(to).href;
	const isExternalHost = hasProtocol(toPath, { acceptRelative: true });
	const isExternal = options?.external || isExternalHost;
	if (isExternal) {
		if (!options?.external) throw navigationDiagnostics.NUXT_E2001({ toPath });
		const { protocol } = new URL(toPath, "http://localhost");
		if (protocol && isScriptProtocol(protocol)) throw navigationDiagnostics.NUXT_E2002({
			toPath,
			protocol
		});
	}
	const inMiddleware = isProcessingMiddleware();
	const router = useRouter();
	const nuxtApp = useNuxtApp();
	if (nuxtApp.ssrContext) {
		const fullPath = typeof to === "string" || isExternal ? toPath : router.resolve(to).fullPath || "/";
		const location = isExternal ? toPath : joinURL((/* @__PURE__ */ useRuntimeConfig()).app.baseURL, fullPath);
		const redirect = async function(response) {
			await nuxtApp.callHook("app:redirected");
			const encodedHeader = encodeURL(location, isExternalHost);
			const encodedLoc = encodeForHtmlAttr(encodedHeader);
			nuxtApp.ssrContext["~renderResponse"] = {
				statusCode: sanitizeStatusCode(options?.redirectCode || 302, 302),
				body: `<!DOCTYPE html><html><head><meta http-equiv="refresh" content="0; url=${encodedLoc}"></head></html>`,
				headers: { location: encodedHeader }
			};
			return response;
		};
		if (!isExternal && inMiddleware) {
			router.afterEach((final) => final.fullPath === fullPath ? redirect(false) : void 0);
			return to;
		}
		return redirect(!inMiddleware ? void 0 : false);
	}
	if (isExternal) {
		nuxtApp._scope.stop();
		if (options?.replace) (void 0).replace(toPath);
		else (void 0).href = toPath;
		if (inMiddleware) {
			if (!nuxtApp.isHydrating) return false;
			return new Promise(() => {});
		}
		return Promise.resolve();
	}
	const encodedTo = typeof to === "string" ? encodeRoutePath(to) : to;
	return options?.replace ? router.replace(encodedTo) : router.push(encodedTo);
};
/**
* @internal
*/
function resolveRouteObject(to) {
	return withQuery(to.path || "", to.query || {}) + (to.hash || "");
}
/**
* @internal
*/
function encodeURL(location, isExternalHost = false) {
	const url = new URL(location, "http://localhost");
	if (!isExternalHost) return url.pathname.replace(/^\/{2,}/, "/") + url.search + url.hash;
	if (location.startsWith("//")) return url.toString().replace(url.protocol, "");
	return url.toString();
}
/**
* Encode the pathname of a route location string. Ensures decoded paths like
* `/café` are percent-encoded to match vue-router's encoded route records.
* Already-encoded paths are not double-encoded.
* @internal
*/
function encodeRoutePath(url) {
	const parsed = parseURL(url);
	return encodePath(decodePath(parsed.pathname)) + parsed.search + parsed.hash;
}
//#endregion
//#region node_modules/nuxt/dist/app/composables/error.js
var NUXT_ERROR_SIGNATURE = "__nuxt_error";
/** @since 3.0.0 */
var useError = /* @__NO_SIDE_EFFECTS__ */ () => toRef(useNuxtApp().payload, "error");
/** @since 3.0.0 */
var showError = (error) => {
	const nuxtError = createError$1(error);
	try {
		const error = /* @__PURE__ */ useError();
		error.value ||= nuxtError;
	} catch {
		throw nuxtError;
	}
	return nuxtError;
};
/** @since 3.0.0 */
var isNuxtError = (error) => !!error && typeof error === "object" && "__nuxt_error" in error;
/** @since 3.0.0 */
var createError$1 = (error) => {
	if (typeof error !== "string" && error.statusText) error.message ??= error.statusText;
	const nuxtError = createError(error);
	Object.defineProperty(nuxtError, NUXT_ERROR_SIGNATURE, {
		value: true,
		configurable: false,
		writable: false
	});
	Object.defineProperty(nuxtError, "status", {
		get: () => nuxtError.statusCode,
		configurable: true
	});
	Object.defineProperty(nuxtError, "statusText", {
		get: () => nuxtError.statusMessage,
		configurable: true
	});
	return nuxtError;
};
//#endregion
//#region virtual:nuxt:node_modules%2F.cache%2Fnuxt%2F.nuxt%2Ffetch.mjs
if (!globalThis.$fetch) globalThis.$fetch = $fetch.create({ baseURL: baseURL() });
var $fetch$1 = globalThis.$fetch;
//#endregion
//#region virtual:nuxt:node_modules%2F.cache%2Fnuxt%2F.nuxt%2Fglobal-polyfills.mjs
if (!("global" in globalThis)) globalThis.global = globalThis;
//#endregion
//#region node_modules/nuxt/dist/head/runtime/island-head.js
/**
* No-op `head.push` until the returned `unfreeze` runs. Plugin/transformer
* augmentations on the same head are unaffected.
*/
function freezeHead(head) {
	const realPush = head.push;
	head.push = () => ({
		dispose: () => {},
		patch: () => {},
		_i: 0
	});
	return () => {
		head.push = realPush;
	};
}
//#endregion
//#region node_modules/nuxt/dist/head/runtime/plugins/unhead.server.js
var plugin$2 = /* @__PURE__ */ defineNuxtPlugin({
	name: "nuxt:head",
	enforce: "pre",
	setup(nuxtApp) {
		const head = nuxtApp.ssrContext.head;
		if (nuxtApp.ssrContext.islandContext) {
			const unfreeze = freezeHead(head);
			nuxtApp.hooks.hookOnce("app:created", unfreeze);
		}
		nuxtApp.vueApp.use(head);
	}
});
//#endregion
//#region node_modules/nuxt/dist/app/diagnostics/manifest.js
/**
* E5xxx
* App manifest / route-rules runtime diagnostics.
*/
var manifestDiagnostics = /* #__PURE__ */ defineProdDiagnostics({
	docsBase,
	reporters: prodReporters
});
//#endregion
//#region virtual:nuxt:node_modules%2F.cache%2Fnuxt%2F.nuxt%2Frouter.options.mjs
var virtual_nuxt_node_modules_2F_cache_2Fnuxt_2F_nuxt_2Frouter_options_default = {};
//#endregion
//#region virtual:nuxt:node_modules%2F.cache%2Fnuxt%2F.nuxt%2Froute-rules.mjs
var sensitiveMatcher = (m, p) => {
	return [];
};
var foldedMatcher = sensitiveMatcher;
var decodeRoutePath = function decodeRoutePath(path) {
	if (!path.includes("%")) return path;
	const queryIndex = path.indexOf("?");
	const pathname = queryIndex === -1 ? path : path.slice(0, queryIndex);
	try {
		return queryIndex === -1 ? decodeURI(pathname) : decodeURI(pathname) + path.slice(queryIndex);
	} catch {
		return path;
	}
};
var normalizePath = (path, fold) => {
	if (typeof path !== "string") return path;
	const decoded = decodeRoutePath(path);
	return fold ? decoded.toLowerCase() : decoded;
};
var virtual_nuxt_node_modules_2F_cache_2Fnuxt_2F_nuxt_2Froute_rules_default = (path) => virtual_nuxt_node_modules_2F_cache_2Fnuxt_2F_nuxt_2Frouter_options_default.sensitive ? defu({}, ...sensitiveMatcher("", normalizePath(path, false)).map((r) => r.data).reverse()) : defu({}, ...foldedMatcher("", normalizePath(path, true)).map((r) => r.data).reverse());
//#endregion
//#region node_modules/nuxt/dist/app/composables/manifest.js
var routeRulesMatcher = virtual_nuxt_node_modules_2F_cache_2Fnuxt_2F_nuxt_2Froute_rules_default;
function getRouteRules(arg) {
	const path = typeof arg === "string" ? arg : arg.path;
	try {
		return routeRulesMatcher(path);
	} catch (e) {
		manifestDiagnostics.NUXT_E5003({
			path,
			cause: e
		});
		return {};
	}
}
//#endregion
//#region virtual:nuxt:node_modules%2F.cache%2Fnuxt%2F.nuxt%2Fmiddleware.mjs
var globalMiddleware = [/* @__PURE__ */ defineNuxtRouteMiddleware((to) => {})];
//#endregion
//#region node_modules/nuxt/dist/app/plugins/router.js
function getRouteFromPath(fullPath) {
	const route = fullPath && typeof fullPath === "object" ? fullPath : {};
	if (typeof fullPath === "object") fullPath = stringifyParsedURL({
		pathname: fullPath.path || "",
		search: stringifyQuery(fullPath.query || {}),
		hash: fullPath.hash || ""
	});
	const url = new URL(fullPath.toString(), "http://localhost");
	return {
		path: url.pathname,
		fullPath,
		query: parseQuery(url.search),
		hash: url.hash,
		params: route.params || {},
		name: void 0,
		matched: route.matched || [],
		redirectedFrom: void 0,
		meta: route.meta || {},
		href: fullPath
	};
}
var plugin$1 = /* @__PURE__ */ defineNuxtPlugin({
	name: "nuxt:router",
	enforce: "pre",
	setup(nuxtApp) {
		const initialURL = nuxtApp.ssrContext.url;
		const routes = [];
		const hooks = {
			"navigate:before": [],
			"resolve:before": [],
			"navigate:after": [],
			"error": []
		};
		const registerHook = (hook, guard) => {
			hooks[hook].push(guard);
			return () => {
				const index = hooks[hook].indexOf(guard);
				if (index !== -1) hooks[hook].splice(index, 1);
			};
		};
		(/* @__PURE__ */ useRuntimeConfig()).app.baseURL;
		const route = reactive(getRouteFromPath(initialURL));
		let navigationCounter = 0;
		async function handleNavigation(url, replace) {
			const navigationId = ++navigationCounter;
			try {
				const to = getRouteFromPath(url);
				for (const middleware of hooks["navigate:before"]) {
					const result = await middleware(to, route);
					if (navigationId !== navigationCounter) return;
					if (result === false || result instanceof Error) return;
					if (typeof result === "string" && result.length) return await handleNavigation(result, true);
				}
				for (const handler of hooks["resolve:before"]) {
					await handler(to, route);
					if (navigationId !== navigationCounter) return;
				}
				Object.assign(route, to);
				for (const middleware of hooks["navigate:after"]) await middleware(to, route);
			} catch (err) {
				for (const handler of hooks.error) await handler(err);
			}
		}
		const router = {
			currentRoute: computed(() => route),
			isReady: () => Promise.resolve(),
			options: {},
			install: () => Promise.resolve(),
			push: (url) => handleNavigation(url, false),
			replace: (url) => handleNavigation(url, true),
			back: () => (void 0).history.go(-1),
			go: (delta) => (void 0).history.go(delta),
			forward: () => (void 0).history.go(1),
			beforeResolve: (guard) => registerHook("resolve:before", guard),
			beforeEach: (guard) => registerHook("navigate:before", guard),
			afterEach: (guard) => registerHook("navigate:after", guard),
			onError: (handler) => registerHook("error", handler),
			resolve: getRouteFromPath,
			addRoute: (parentName, route) => {
				routes.push(route);
			},
			getRoutes: () => routes,
			hasRoute: (name) => routes.some((route) => route.name === name),
			removeRoute: (name) => {
				const index = routes.findIndex((route) => route.name === name);
				if (index !== -1) routes.splice(index, 1);
			}
		};
		nuxtApp.vueApp.component("RouterLink", defineComponent({
			functional: true,
			props: {
				to: {
					type: String,
					required: true
				},
				custom: Boolean,
				replace: Boolean,
				activeClass: String,
				exactActiveClass: String,
				ariaCurrentValue: String
			},
			setup: (props, { slots }) => {
				const navigate = () => handleNavigation(props.to, props.replace);
				return () => {
					const route = router.resolve(props.to);
					return props.custom ? slots.default?.({
						href: props.to,
						navigate,
						route
					}) : h("a", {
						href: props.to,
						onClick: (e) => {
							e.preventDefault();
							return navigate();
						}
					}, slots);
				};
			}
		}));
		nuxtApp._route = route;
		nuxtApp._middleware ||= {
			global: [],
			named: {}
		};
		const initialLayout = nuxtApp.payload.state._layout;
		const initialLayoutProps = nuxtApp.payload.state._layoutProps;
		nuxtApp.hooks.hookOnce("app:created", async () => {
			router.beforeEach(async (to, from) => {
				to.meta = reactive(to.meta || {});
				if (nuxtApp.isHydrating && initialLayout && !isReadonly(to.meta.layout)) {
					to.meta.layout = initialLayout;
					to.meta.layoutProps = initialLayoutProps;
				}
				nuxtApp._processingMiddleware = true;
				nuxtApp._middlewareTo = to;
				if (!nuxtApp.ssrContext?.islandContext) {
					const middlewareEntries = /* @__PURE__ */ new Set([...globalMiddleware, ...nuxtApp._middleware.global]);
					const routeRules = getRouteRules({ path: to.path });
					if (routeRules.appMiddleware) for (const key in routeRules.appMiddleware) {
						const guard = nuxtApp._middleware.named[key];
						if (!guard) continue;
						if (routeRules.appMiddleware[key]) middlewareEntries.add(guard);
						else middlewareEntries.delete(guard);
					}
					for (const middleware of middlewareEntries) {
						const result = await nuxtApp.runWithContext(() => middleware(to, from));
						if (result === false || result instanceof Error) {
							const error = result || createError({
								status: 404,
								statusText: `Page Not Found: ${initialURL}`,
								data: { path: initialURL }
							});
							delete nuxtApp._processingMiddleware;
							delete nuxtApp._middlewareTo;
							return nuxtApp.runWithContext(() => showError(error));
						}
						if (result === true) continue;
						if (result || result === false) return result;
					}
				}
			});
			router.afterEach(() => {
				delete nuxtApp._processingMiddleware;
				delete nuxtApp._middlewareTo;
			});
			await router.replace(initialURL);
			if (!isEqual(route.fullPath, initialURL)) await nuxtApp.runWithContext(() => navigateTo(route.fullPath));
		});
		return { provide: {
			route,
			router
		} };
	}
});
//#endregion
//#region node_modules/nuxt/dist/app/diagnostics/head.js
/**
* E6xxx
* Head / unhead runtime diagnostics.
*/
var unheadDiagnostics = /* #__PURE__ */ defineProdDiagnostics({
	docsBase,
	reporters: prodReporters
});
//#endregion
//#region node_modules/nuxt/dist/head/runtime/composables.js
/**
* Injects the head client from the Nuxt context or Vue inject.
*/
function injectHead(nuxtApp) {
	const nuxt = nuxtApp || useNuxtApp();
	return nuxt.ssrContext?.head || nuxt.runWithContext(() => {
		if (hasInjectionContext()) {
			const head = inject(headSymbol);
			if (!head) throw unheadDiagnostics.NUXT_E6001();
			return head;
		}
	});
}
function useHead$1(input, options = {}) {
	const head = options.head || injectHead(options.nuxt);
	return useHead(input, {
		head,
		...options
	});
}
//#endregion
//#region node_modules/nuxt/dist/app/diagnostics/state.js
/**
* E7xxx
* Payload / state / cookie runtime diagnostics.
*/
var stateDiagnostics = /* #__PURE__ */ defineProdDiagnostics({
	docsBase,
	reporters: prodReporters
});
//#endregion
//#region node_modules/nuxt/dist/app/composables/payload.js
/**
* This is an experimental function for configuring passing rich data from server -> client.
* @since 3.4.0
*/
function definePayloadReducer(name, reduce) {
	useNuxtApp().ssrContext["~payloadReducers"][name] = reduce;
}
//#endregion
//#region node_modules/nuxt/dist/app/plugins/revive-payload.server.js
var reducers = [
	["NuxtError", (data) => isNuxtError(data) && data.toJSON()],
	["EmptyShallowRef", (data) => isRef(data) && isShallow(data) && !data.value && (typeof data.value === "bigint" ? "0n" : JSON.stringify(data.value) || "_")],
	["EmptyRef", (data) => isRef(data) && !data.value && (typeof data.value === "bigint" ? "0n" : JSON.stringify(data.value) || "_")],
	["ShallowRef", (data) => isRef(data) && isShallow(data) && data.value],
	["ShallowReactive", (data) => isReactive(data) && isShallow(data) && toRaw(data)],
	["Ref", (data) => isRef(data) && data.value],
	["Reactive", (data) => isReactive(data) && toRaw(data)]
];
//#endregion
//#region virtual:nuxt:node_modules%2F.cache%2Fnuxt%2F.nuxt%2Fplugins.server.mjs
var virtual_nuxt_node_modules_2F_cache_2Fnuxt_2F_nuxt_2Fplugins_server_default = [
	plugin$2,
	plugin$1,
	/* @__PURE__ */ defineNuxtPlugin({
		name: "nuxt:revive-payload:server",
		setup() {
			for (const [reducer, fn] of reducers) definePayloadReducer(reducer, fn);
		}
	}),
	/* @__PURE__ */ defineNuxtPlugin({ name: "nuxt:global-components" })
];
//#endregion
//#region node_modules/nuxt/dist/app/composables/state.js
var useStateKeyPrefix = "$s";
function useState(...args) {
	const autoKey = typeof args[args.length - 1] === "string" ? args.pop() : void 0;
	if (typeof args[0] !== "string") args.unshift(autoKey);
	const [_key, init] = args;
	if (!_key || typeof _key !== "string") throw stateDiagnostics.NUXT_E7009({ key: _key });
	if (init !== void 0 && typeof init !== "function") throw stateDiagnostics.NUXT_E7007({ type: typeof init });
	const key = useStateKeyPrefix + _key;
	const nuxtApp = useNuxtApp();
	const state = toRef(nuxtApp.payload.state, key);
	if (init) nuxtApp._state[key] ??= { _default: init };
	if (state.value === void 0 && init) {
		const initialValue = init();
		if (isRef(initialValue)) {
			nuxtApp.payload.state[key] = initialValue;
			return initialValue;
		}
		state.value = initialValue;
	}
	return state;
}
//#endregion
//#region composables/useTracker.ts
var defaultProjects = [
	"Project Alpha",
	"Website Redesign",
	"Mobile App",
	"Infrastructure"
];
var defaultUsers = [
	"TEZ",
	"John",
	"Alex",
	"Sarah"
];
var defaultTags = [
	"Frontend",
	"Backend",
	"UI/UX",
	"Bug Fix",
	"Feature",
	"Meeting",
	"Refactor"
];
var defaultNotifications = [{
	id: "1",
	title: "System Initialized",
	message: "Welcome to your work ecosystem tracker.",
	time: "Just now",
	type: "info",
	read: false
}, {
	id: "2",
	title: "Sprint Deadline",
	message: "Month-end reports review scheduled for Friday.",
	time: "2h ago",
	type: "warning",
	read: false
}];
var getInitialLogs = () => {
	const todayStr = (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
	const yDate = /* @__PURE__ */ new Date();
	yDate.setDate(yDate.getDate() - 1);
	const yesterdayStr = yDate.toISOString().slice(0, 10);
	return [
		{
			id: "demo-log-1",
			date: todayStr,
			user: "TEZ",
			users: ["TEZ"],
			text: "Navigation UI Refactor · 1.0h · Project Alpha",
			project: "Project Alpha",
			task: "Navigation UI Refactor",
			hours: 1,
			startTime: "10:00",
			endTime: "11:00",
			tags: ["Frontend", "UI/UX"]
		},
		{
			id: "demo-log-2",
			date: todayStr,
			user: "TEZ, Alex",
			users: ["TEZ", "Alex"],
			text: "API Endpoints & Validation · 1.5h · Website Redesign",
			project: "Website Redesign",
			task: "API Endpoints & Validation",
			hours: 1.5,
			startTime: "11:30",
			endTime: "13:00",
			tags: ["Backend", "Bug Fix"]
		},
		{
			id: "demo-log-3",
			date: yesterdayStr,
			user: "John, TEZ",
			users: ["John", "TEZ"],
			text: "Database Query Optimization · 2.0h · Infrastructure",
			project: "Infrastructure",
			task: "Database Query Optimization",
			hours: 2,
			startTime: "14:00",
			endTime: "16:00",
			tags: ["Backend", "Refactor"]
		}
	];
};
var oneYearFromNow = () => {
	const d = /* @__PURE__ */ new Date();
	d.setFullYear(d.getFullYear() + 1);
	return d.toISOString().slice(0, 10);
};
var defaultApiKeys = [{
	id: "key-demo-1",
	name: "Office Timesheets Connector",
	key: "mytrk_live_sk_9a8f7e6d5c4b3a21",
	createdDate: (/* @__PURE__ */ new Date()).toISOString().slice(0, 10),
	expiryDate: oneYearFromNow(),
	expirationOption: "1year",
	scopes: [
		"Read Logs",
		"Write Entries",
		"Auto Sync"
	],
	lastUsed: "Just now",
	status: "active"
}, {
	id: "key-demo-2",
	name: "Figma Design System Sync",
	key: "mytrk_live_sk_3f1d8c9a2b5e4017",
	createdDate: (/* @__PURE__ */ new Date()).toISOString().slice(0, 10),
	expiryDate: oneYearFromNow(),
	expirationOption: "1year",
	scopes: ["Read Logs", "Write Entries"],
	lastUsed: "2h ago",
	status: "active"
}];
var defaultWorkspaceConnections = [
	{
		id: "conn-office",
		name: "Office Timesheets Enterprise",
		type: "officetimesheets",
		icon: "🏢",
		accountOrWorkspace: "Acme Corp (v4.2 Endpoint)",
		status: "connected",
		autoSync: true,
		lastSynced: "Today at 10:30 AM",
		details: { endpoint: "https://api.officetimesheets.com/v1/sync" }
	},
	{
		id: "conn-figma",
		name: "Figma Design Team",
		type: "figma",
		icon: "🎨",
		accountOrWorkspace: "Product UI System (Team TEZ)",
		status: "connected",
		autoSync: true,
		lastSynced: "15m ago",
		details: {
			teamId: "figma_team_99182",
			autoLogFrames: true
		}
	},
	{
		id: "conn-mcp",
		name: "MCP Local AI Context Protocol",
		type: "mcp",
		icon: "🤖",
		accountOrWorkspace: "Antigravity / Cursor Agent Node",
		status: "connected",
		autoSync: true,
		lastSynced: "Just now",
		details: {
			port: 3e3,
			streamLogs: true
		}
	},
	{
		id: "conn-notion",
		name: "Notion Workspace",
		type: "notion",
		icon: "📝",
		accountOrWorkspace: "TEZ Engineering & Product Docs",
		status: "connected",
		autoSync: false,
		lastSynced: "Yesterday at 4:15 PM",
		details: { dbId: "notion_db_881920" }
	}
];
var defaultOfficeIntegration = {
	enabled: true,
	endpoint: "https://api.officetimesheets.com/v1/sync",
	apiKey: "ots_secret_88x99z22k11",
	autoSync: true,
	lastSynced: "Today at 10:30 AM",
	status: "connected"
};
var initial = {
	tasks: [],
	osg: [],
	logs: getInitialLogs(),
	todos: [],
	feedback: [],
	meetings: [],
	projects: defaultProjects,
	users: defaultUsers,
	tags: defaultTags,
	notifications: defaultNotifications,
	apiKeys: defaultApiKeys,
	officeIntegration: defaultOfficeIntegration,
	workspaceConnections: defaultWorkspaceConnections
};
function useTracker() {
	const data = useState("tracker-data", () => structuredClone(initial));
	useState("tracker-loaded", () => false);
	const config = /* @__PURE__ */ useRuntimeConfig();
	const save = () => {};
	const load = () => {};
	const addTask = (task) => {
		data.value.tasks.push({
			...task,
			id: crypto.randomUUID()
		});
		if (task.project && !data.value.projects.includes(task.project)) data.value.projects.push(task.project);
	};
	const addOsg = (item) => {
		data.value.osg.push({
			...item,
			id: crypto.randomUUID()
		});
	};
	const addLog = (entry) => {
		const isAutoSync = data.value.officeIntegration?.enabled && data.value.officeIntegration?.autoSync;
		const newLog = {
			...entry,
			id: crypto.randomUUID(),
			officeSynced: isAutoSync ? true : false,
			officeSyncTime: isAutoSync ? (/* @__PURE__ */ new Date()).toLocaleTimeString([], {
				hour: "2-digit",
				minute: "2-digit"
			}) : void 0
		};
		data.value.logs.push(newLog);
		if (entry.project && !data.value.projects.includes(entry.project)) data.value.projects.push(entry.project);
		if (isAutoSync) addNotification({
			title: "Office Timesheets Synced",
			message: `Work log "${entry.task || "Time Entry"}" pushed to Office Timesheets.`,
			time: "Just now",
			type: "success",
			read: false
		});
	};
	const updateLog = (log) => {
		const i = data.value.logs.findIndex((x) => x.id === log.id);
		if (i >= 0) {
			const isAutoSync = data.value.officeIntegration?.enabled && data.value.officeIntegration?.autoSync;
			data.value.logs[i] = {
				...log,
				officeSynced: isAutoSync ? true : log.officeSynced,
				officeSyncTime: isAutoSync ? (/* @__PURE__ */ new Date()).toLocaleTimeString([], {
					hour: "2-digit",
					minute: "2-digit"
				}) : log.officeSyncTime
			};
			if (log.project && !data.value.projects.includes(log.project)) data.value.projects.push(log.project);
		}
	};
	const removeLog = (id) => {
		data.value.logs = data.value.logs.filter((x) => x.id !== id);
	};
	const addProject = (projectName) => {
		const trimmed = projectName.trim();
		if (trimmed && !data.value.projects.includes(trimmed)) data.value.projects.push(trimmed);
	};
	const removeProject = (projectName) => {
		data.value.projects = data.value.projects.filter((p) => p !== projectName);
	};
	const addUser = (userName) => {
		const trimmed = userName.trim();
		if (trimmed && !data.value.users.includes(trimmed)) data.value.users.push(trimmed);
	};
	const removeUser = (userName) => {
		data.value.users = data.value.users.filter((u) => u !== userName);
	};
	const addTag = (tagName) => {
		const trimmed = tagName.trim();
		if (!data.value.tags) data.value.tags = [...defaultTags];
		if (trimmed && !data.value.tags.includes(trimmed)) data.value.tags.push(trimmed);
	};
	const removeTag = (tagName) => {
		if (!data.value.tags) data.value.tags = [...defaultTags];
		data.value.tags = data.value.tags.filter((t) => t !== tagName);
	};
	const updateTask = (task) => {
		const i = data.value.tasks.findIndex((x) => x.id === task.id);
		if (i >= 0) data.value.tasks[i] = task;
	};
	const removeTask = (id) => {
		data.value.tasks = data.value.tasks.filter((x) => x.id !== id);
	};
	const addNotification = (notif) => {
		data.value.notifications.unshift({
			...notif,
			id: crypto.randomUUID()
		});
	};
	const markNotificationRead = (id) => {
		const target = data.value.notifications.find((n) => n.id === id);
		if (target) target.read = true;
	};
	const clearNotifications = () => {
		data.value.notifications = [];
	};
	const generateApiKey = (name, expirationOption = "1year", scopes = ["Read Logs", "Write Entries"]) => {
		const keyVal = `mytrk_live_sk_${Array.from({ length: 16 }, () => Math.floor(Math.random() * 16).toString(16)).join("")}`;
		let expiryDate = void 0;
		const now = /* @__PURE__ */ new Date();
		if (expirationOption === "1year") {
			now.setFullYear(now.getFullYear() + 1);
			expiryDate = now.toISOString().slice(0, 10);
		} else if (expirationOption === "30days") {
			now.setDate(now.getDate() + 30);
			expiryDate = now.toISOString().slice(0, 10);
		} else if (expirationOption === "90days") {
			now.setDate(now.getDate() + 90);
			expiryDate = now.toISOString().slice(0, 10);
		}
		const newKey = {
			id: crypto.randomUUID(),
			name: name.trim() || "Custom API Key",
			key: keyVal,
			createdDate: (/* @__PURE__ */ new Date()).toISOString().slice(0, 10),
			expiryDate,
			expirationOption,
			scopes,
			lastUsed: "Never",
			status: "active"
		};
		if (!data.value.apiKeys) data.value.apiKeys = [];
		data.value.apiKeys.unshift(newKey);
		return newKey;
	};
	const revokeApiKey = (id) => {
		if (data.value.apiKeys) {
			const target = data.value.apiKeys.find((k) => k.id === id);
			if (target) target.status = "revoked";
		}
	};
	const deleteApiKey = (id) => {
		if (data.value.apiKeys) data.value.apiKeys = data.value.apiKeys.filter((k) => k.id !== id);
	};
	const updateOfficeIntegration = (updated) => {
		data.value.officeIntegration = {
			...data.value.officeIntegration || defaultOfficeIntegration,
			...updated
		};
	};
	const syncOfficeTimesheets = async () => {
		if (!data.value.officeIntegration) return {
			success: false,
			syncedCount: 0
		};
		data.value.officeIntegration.status = "syncing";
		await new Promise((r) => setTimeout(r, 600));
		let count = 0;
		data.value.logs.forEach((l) => {
			if (!l.officeSynced) {
				l.officeSynced = true;
				l.officeSyncTime = (/* @__PURE__ */ new Date()).toLocaleTimeString([], {
					hour: "2-digit",
					minute: "2-digit"
				});
				count++;
			}
		});
		const nowStr = `Today at ${(/* @__PURE__ */ new Date()).toLocaleTimeString([], {
			hour: "2-digit",
			minute: "2-digit"
		})}`;
		data.value.officeIntegration.status = "connected";
		data.value.officeIntegration.lastSynced = nowStr;
		addNotification({
			title: "Bi-directional Sync Complete",
			message: `Successfully synchronized ${count} timesheet log(s) with Office Timesheets.`,
			time: "Just now",
			type: "success",
			read: false
		});
		return {
			success: true,
			syncedCount: count
		};
	};
	const syncSheets = async () => {
		if (!config.public.sheetsEndpoint) return false;
		await $fetch$1(config.public.sheetsEndpoint, {
			method: "POST",
			body: data.value
		});
		return true;
	};
	return {
		data,
		load,
		save,
		addTask,
		addOsg,
		addLog,
		updateLog,
		removeLog,
		addProject,
		removeProject,
		addUser,
		removeUser,
		addTag,
		removeTag,
		updateTask,
		removeTask,
		addNotification,
		markNotificationRead,
		clearNotifications,
		generateApiKey,
		revokeApiKey,
		deleteApiKey,
		updateOfficeIntegration,
		syncOfficeTimesheets,
		syncSheets,
		hasSheets: computed(() => Boolean(config.public.sheetsEndpoint))
	};
}
//#endregion
//#region app.vue?vue&type=script&setup=true&lang.ts
var app_vue_vue_type_script_setup_true_lang_default = /*@__PURE__*/ defineComponent({
	__name: "app",
	__ssrInlineRender: true,
	setup(__props) {
		const { data, load, addTask, updateTask, addLog, updateLog, removeLog, addProject, removeProject, addUser, removeUser, addTag, removeTag: deleteTagFromTracker, save, addNotification, markNotificationRead, clearNotifications, generateApiKey, revokeApiKey, updateOfficeIntegration, syncOfficeTimesheets } = useTracker();
		const isTestingConnection = ref(false);
		const isSyncingOffice = ref(false);
		const showOfficeApiKey = ref(false);
		ref("");
		const newlyCreatedKeyObj = ref(null);
		const copySuccessId = ref(null);
		const activeIntegrationTab = ref("workspaces");
		const showGenerateKeyModal = ref(false);
		const keyModalForm = ref({
			name: "",
			expirationOption: "1year",
			scopes: ["Read Logs", "Write Entries"]
		});
		const createdKeySecretObj = ref(null);
		const keyToRevoke = ref(null);
		const activeConnectorModal = ref(null);
		const figmaForm = ref({
			teamId: "figma_team_99182",
			autoLogFrames: true
		});
		const mcpForm = ref({
			serverUrl: "http://localhost:3000/mcp",
			streamLogs: true
		});
		const notionForm = ref({
			databaseId: "notion_db_881920",
			syncPages: true
		});
		const officeForm = ref({
			endpoint: data.value.officeIntegration?.endpoint || "https://api.officetimesheets.com/v1/sync",
			apiKey: data.value.officeIntegration?.apiKey || "ots_secret_88x99z22k11",
			autoSync: data.value.officeIntegration?.autoSync ?? true,
			enabled: data.value.officeIntegration?.enabled ?? true
		});
		const screen = ref("dashboard");
		const month = ref((/* @__PURE__ */ new Date()).toISOString().slice(0, 7));
		const modal = ref("");
		const search = ref("");
		const dark = ref(false);
		const collapsed = ref(false);
		const profileOpen = ref(false);
		const notifOpen = ref(false);
		const addNewOpen = ref(false);
		const showMonthPicker = ref(false);
		const showLogsMonthPicker = ref(false);
		const showTodoMonthPicker = ref(false);
		const todoSelectedDay = ref(null);
		const pickerYear = ref((/* @__PURE__ */ new Date()).getFullYear());
		const toast = ref("");
		const importOpen = ref(false);
		const importPreview = ref("");
		ref(null);
		ref([]);
		ref(null);
		const timer = ref(null);
		const elapsed = ref(0);
		const timerTask = ref("");
		const timerProject = ref("");
		const predefinedTags = [
			"Frontend",
			"Backend",
			"UI/UX",
			"Bug Fix",
			"Feature",
			"Meeting",
			"Refactor"
		];
		const availableTags = computed(() => data.value.tags && data.value.tags.length ? data.value.tags : predefinedTags);
		const tagInputText = ref("");
		const personInputText = ref("");
		const task = ref({
			project: "",
			category: "General",
			task: "",
			owner: "TEZ",
			owners: ["TEZ"],
			status: "In Progress",
			tags: [],
			startDate: (/* @__PURE__ */ new Date()).toISOString().slice(0, 10),
			endDate: (/* @__PURE__ */ new Date()).toISOString().slice(0, 10),
			startTime: "09:00",
			endTime: "17:00"
		});
		ref(null);
		const editingTodo = ref(null);
		const todo = ref({
			title: "",
			detail: "",
			due: (/* @__PURE__ */ new Date()).toISOString().slice(0, 10),
			status: "Pending",
			priority: "Medium",
			link: "",
			fileName: "",
			fileData: ""
		});
		const months = Array.from({ length: 12 }, (_, i) => `${(/* @__PURE__ */ new Date()).getFullYear()}-${String(i + 1).padStart(2, "0")}`);
		const monthNamesShort = [
			"Jan",
			"Feb",
			"Mar",
			"Apr",
			"May",
			"Jun",
			"Jul",
			"Aug",
			"Sep",
			"Oct",
			"Nov",
			"Dec"
		];
		const monthLabel = computed(() => (/* @__PURE__ */ new Date(`${month.value}-01`)).toLocaleDateString("en-US", {
			month: "long",
			year: "numeric"
		}));
		const greeting = computed(() => {
			const hour = (/* @__PURE__ */ new Date()).getHours();
			if (hour >= 5 && hour < 12) return "Good morning";
			if (hour >= 12 && hour < 17) return "Good afternoon";
			if (hour >= 17 && hour < 22) return "Good evening";
			return "Good night";
		});
		const monthTasks = computed(() => data.value.tasks.filter((t) => t.month === month.value && Object.values(t).join(" ").toLowerCase().includes(search.value.toLowerCase())));
		const hours = computed(() => monthTasks.value.reduce((s, t) => s + Number(t.hours || 0), 0));
		const done = computed(() => monthTasks.value.filter((t) => t.status === "Completed").length);
		computed(() => {
			if (task.value.startTime && task.value.endTime) {
				const [startH, startM] = task.value.startTime.split(":").map(Number);
				const [endH, endM] = task.value.endTime.split(":").map(Number);
				const diffMinutes = endH * 60 + endM - (startH * 60 + startM);
				if (diffMinutes > 0) return Math.round(diffMinutes / 60 * 10) / 10;
			}
			return 1;
		});
		const chartRange = ref("day");
		const chartDataPoints = computed(() => {
			if (chartRange.value === "day") {
				const [yearStr, monthStr] = month.value.split("-");
				const yearNum = Number(yearStr);
				const monthNum = Number(monthStr);
				const daysInMonth = new Date(yearNum, monthNum, 0).getDate();
				const mName = monthNamesShort[monthNum - 1];
				const result = [];
				for (let d = 1; d <= daysInMonth; d++) {
					const dayStr = String(d).padStart(2, "0");
					const fullDate = `${month.value}-${dayStr}`;
					const dayLabel = `${d} ${mName}`;
					const taskHrs = data.value.tasks.filter((t) => t.startDate === fullDate || t.due === fullDate || t.month === month.value && Number(t.startDate?.slice(-2)) === d).reduce((sum, t) => sum + Number(t.hours || 0), 0);
					const logHrs = data.value.logs.filter((l) => l.date === fullDate).reduce((sum, l) => sum + Number(l.hours || 0), 0);
					const totalHrs = Math.max(taskHrs, logHrs) || (d % 3 === 0 ? Math.round((d % 5 + 1) * 10) / 10 : 0);
					result.push({
						label: dayLabel,
						hours: totalHrs,
						date: fullDate
					});
				}
				return result;
			} else if (chartRange.value === "week") return [
				"Mon",
				"Tue",
				"Wed",
				"Thu",
				"Fri",
				"Sat",
				"Sun"
			].map((dayName, idx) => {
				return {
					label: dayName,
					hours: monthTasks.value.filter((_, tIdx) => tIdx % 7 === idx).reduce((s, t) => s + Number(t.hours || 0), 0) || Math.round((hours.value / 7 + idx % 3) * 10) / 10
				};
			});
			else return monthNamesShort.map((mName, mIdx) => {
				const mStr = `${(/* @__PURE__ */ new Date()).getFullYear()}-${String(mIdx + 1).padStart(2, "0")}`;
				return {
					label: mName,
					hours: data.value.tasks.filter((t) => t.month === mStr).reduce((s, t) => s + Number(t.hours || 0), 0) || (mStr === month.value ? hours.value : 0),
					monthKey: mStr
				};
			});
		});
		const maxChartHours = computed(() => {
			const maxVal = Math.max(...chartDataPoints.value.map((p) => p.hours), 1);
			return Math.ceil(maxVal / 4) * 4 || 8;
		});
		const monthChartPoints = computed(() => {
			if (chartRange.value !== "month") return [];
			const pts = chartDataPoints.value;
			const max = maxChartHours.value || 1;
			return pts.map((p, i) => {
				const x = Math.round(35 + i / (pts.length - 1 || 1) * 930);
				const y = Math.round(185 - p.hours / max * 155);
				return {
					...p,
					x,
					y
				};
			});
		});
		const monthChartSvgPath = computed(() => {
			const pts = monthChartPoints.value;
			if (!pts.length) return {
				line: "",
				area: ""
			};
			let d = `M ${pts[0].x} ${pts[0].y}`;
			for (let i = 0; i < pts.length - 1; i++) {
				const p0 = pts[i === 0 ? 0 : i - 1];
				const p1 = pts[i];
				const p2 = pts[i + 1];
				const p3 = pts[i + 2] || p2;
				const cp1x = p1.x + (p2.x - p0.x) / 6;
				const cp1y = p1.y + (p2.y - p0.y) / 6;
				const cp2x = p2.x - (p3.x - p1.x) / 6;
				const cp2y = p2.y - (p3.y - p1.y) / 6;
				d += ` C ${cp1x.toFixed(1)} ${cp1y.toFixed(1)}, ${cp2x.toFixed(1)} ${cp2y.toFixed(1)}, ${p2.x} ${p2.y}`;
			}
			const last = pts[pts.length - 1];
			const first = pts[0];
			const area = `${d} L ${last.x} 195 L ${first.x} 195 Z`;
			return {
				line: d,
				area
			};
		});
		const todoStats = computed(() => {
			const pending = data.value.todos.filter((t) => t.status === "Pending").length;
			const inProgress = data.value.todos.filter((t) => t.status === "In Progress").length;
			const completed = data.value.todos.filter((t) => t.status === "Completed").length;
			const total = pending + inProgress + completed || 1;
			return {
				pending,
				inProgress,
				completed,
				total: pending + inProgress + completed,
				pendingPct: Math.round(pending / total * 100),
				inProgressPct: Math.round(inProgress / total * 100),
				completedPct: Math.round(completed / total * 100),
				pendingStroke: `${pending / total * 251.327} 251.327`,
				inProgressStroke: `${inProgress / total * 251.327} 251.327`,
				completedStroke: `${completed / total * 251.327} 251.327`,
				inProgressOffset: -(pending / total * 251.327),
				completedOffset: -((pending + inProgress) / total * 251.327)
			};
		});
		const activeDropdown = ref(null);
		const filteredProjects = computed(() => {
			const q = (task.value.project || "").toLowerCase().trim();
			if (!q) return data.value.projects;
			return data.value.projects.filter((p) => p.toLowerCase().includes(q));
		});
		const filteredUsers = computed(() => {
			const q = personInputText.value.toLowerCase().trim();
			if (!q) return data.value.users;
			return data.value.users.filter((u) => u.toLowerCase().includes(q));
		});
		const filteredTags = computed(() => {
			const q = tagInputText.value.toLowerCase().trim();
			const allAvailable = Array.from(/* @__PURE__ */ new Set([...availableTags.value, ...task.value.tags]));
			if (!q) return allAvailable;
			return allAvailable.filter((t) => t.toLowerCase().includes(q));
		});
		const isAdmin = (name) => {
			if (!name) return false;
			const n = name.trim().toLowerCase();
			return n === "tez" || n === "admin";
		};
		const avatarPalette = [
			"#3569df",
			"#11a981",
			"#ef9b27",
			"#8a3ffc",
			"#e53e3e",
			"#009d9a",
			"#d12771"
		];
		const getAvatarColor = (name) => {
			if (!name) return "#3569df";
			if (isAdmin(name)) return "#3569df";
			let hash = 0;
			for (let i = 0; i < name.length; i++) hash = name.charCodeAt(i) + ((hash << 5) - hash);
			return avatarPalette[Math.abs(hash) % avatarPalette.length];
		};
		const formatDynamicDuration = (hoursNum) => {
			const h = Number(hoursNum) || 0;
			if (h <= 0) return "0m";
			const totalMinutes = Math.round(h * 60);
			const hrs = Math.floor(totalMinutes / 60);
			const mins = totalMinutes % 60;
			if (hrs === 0) return `${mins}m`;
			if (mins === 0) return `${hrs}h`;
			return `${hrs}h ${mins}m`;
		};
		const getLogTimeRange = (item) => {
			if (item.startTime && item.endTime) return `${item.startTime} - ${item.endTime}`;
			if (item.startTime) {
				const [sh, sm] = item.startTime.split(":").map(Number);
				const endMin = (sh || 10) * 60 + (sm || 0) + Math.round((Number(item.hours) || 1) * 60);
				const eh = Math.floor(endMin / 60) % 24;
				const em = endMin % 60;
				return `${item.startTime} - ${String(eh).padStart(2, "0")}:${String(em).padStart(2, "0")}`;
			}
			const h = Number(item.hours) || 1;
			const eh = Math.min(23, 10 + Math.floor(h));
			const em = Math.round(h % 1 * 60);
			return `10:00 - ${String(eh).padStart(2, "0")}:${String(em).padStart(2, "0")}`;
		};
		const toggledTimeLogIds = ref({});
		const isTimeToggled = (id) => {
			return Boolean(toggledTimeLogIds.value[id]);
		};
		ref(null);
		ref("10:00");
		ref("11:00");
		ref(1);
		const selectedLogIds = ref([]);
		const exportOnlySelected = ref(false);
		const bulkMenuOpenGroupDate = ref(null);
		const isLogSelected = (id) => selectedLogIds.value.includes(id);
		const isGroupAllSelected = (group) => {
			return group.items.length > 0 && group.items.every((item) => selectedLogIds.value.includes(item.id));
		};
		const getGroupSelectedCount = (group) => {
			return group.items.filter((item) => selectedLogIds.value.includes(item.id)).length;
		};
		const groupedLogsByDate = computed(() => {
			const groupsMap = /* @__PURE__ */ new Map();
			const sorted = [...data.value.logs].sort((a, b) => {
				const da = a.date || "";
				const db = b.date || "";
				if (da === db) return (b.startTime || "").localeCompare(a.startTime || "");
				return db.localeCompare(da);
			});
			for (const log of sorted) {
				const d = log.date || (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
				if (!groupsMap.has(d)) groupsMap.set(d, []);
				groupsMap.get(d).push(log);
			}
			const todayStr = (/* @__PURE__ */ new Date()).toISOString().slice(0, 10);
			const yDate = /* @__PURE__ */ new Date();
			yDate.setDate(yDate.getDate() - 1);
			const yesterdayStr = yDate.toISOString().slice(0, 10);
			const result = [];
			for (const [dateStr, items] of groupsMap.entries()) {
				const isToday = dateStr === todayStr;
				const isYesterday = dateStr === yesterdayStr;
				const formatted = formatDateDMY(dateStr);
				const relative = isToday ? `Today · ${formatted}` : isYesterday ? `Yesterday · ${formatted}` : formatted;
				const totalHours = items.reduce((acc, it) => acc + Number(it.hours || 0), 0);
				const formattedTotal = formatDynamicDuration(totalHours);
				result.push({
					date: dateStr,
					formattedDate: formatted,
					isToday,
					isYesterday,
					relativeLabel: relative,
					totalHours,
					formattedTotalDuration: formattedTotal,
					items
				});
			}
			return result;
		});
		const getLogUsers = (item) => {
			if (item.users && item.users.length) return item.users;
			if (item.user) return item.user.split(",").map((s) => s.trim()).filter(Boolean);
			return ["TEZ"];
		};
		const tone = (v) => v ? v.toLowerCase().replaceAll(" ", "-") : "default";
		const formatDateDMY = (dateStr) => {
			if (!dateStr) return "-";
			try {
				const parts = dateStr.split("-");
				if (parts.length === 3) {
					const y = parts[0];
					const m = parseInt(parts[1], 10);
					return `${parseInt(parts[2], 10)} ${monthNamesShort[m - 1] || parts[1]}, ${y}`;
				}
				const dt = new Date(dateStr);
				if (!isNaN(dt.getTime())) return dt.toLocaleDateString("en-GB", {
					day: "numeric",
					month: "short",
					year: "numeric"
				});
				return dateStr;
			} catch {
				return dateStr;
			}
		};
		const activeEditDropdown = ref(null);
		const editPersonInputText = ref("");
		const editTagInputText = ref("");
		const editLogData = ref({
			id: "",
			task: "",
			project: "",
			owners: ["TEZ"],
			tags: [],
			startDate: (/* @__PURE__ */ new Date()).toISOString().slice(0, 10),
			endDate: (/* @__PURE__ */ new Date()).toISOString().slice(0, 10),
			startTime: "09:00",
			endTime: "17:00",
			hours: 1
		});
		const filteredEditProjects = computed(() => {
			const q = (editLogData.value.project || "").toLowerCase().trim();
			if (!q) return data.value.projects;
			return data.value.projects.filter((p) => p.toLowerCase().includes(q));
		});
		const filteredEditUsers = computed(() => {
			const q = editPersonInputText.value.toLowerCase().trim();
			if (!q) return data.value.users;
			return data.value.users.filter((u) => u.toLowerCase().includes(q));
		});
		const filteredEditTags = computed(() => {
			const q = editTagInputText.value.toLowerCase().trim();
			const allAvailable = Array.from(/* @__PURE__ */ new Set([...availableTags.value, ...editLogData.value.tags]));
			if (!q) return allAvailable;
			return allAvailable.filter((t) => t.toLowerCase().includes(q));
		});
		computed(() => {
			if (editLogData.value.startTime && editLogData.value.endTime) {
				const [startH, startM] = editLogData.value.startTime.split(":").map(Number);
				const [endH, endM] = editLogData.value.endTime.split(":").map(Number);
				const diffMinutes = endH * 60 + endM - (startH * 60 + startM);
				if (diffMinutes > 0) return Math.round(diffMinutes / 60 * 10) / 10;
			}
			return editLogData.value.hours || 1;
		});
		ref(null);
		ref(null);
		const getDisplayUrl = (url) => {
			if (!url) return "";
			try {
				const clean = url.replace(/^https?:\/\//, "").replace(/^www\./, "");
				return clean.length > 22 ? clean.slice(0, 20) + "..." : clean;
			} catch {
				return "Link";
			}
		};
		const currentMonthTodos = computed(() => {
			return data.value.todos.filter((t) => {
				if (t.due && t.due.startsWith(month.value)) return true;
				if (t.createdDate && t.createdDate.startsWith(month.value)) return true;
				if (t.month === month.value) return true;
				if (!t.due && !t.month && month.value === (/* @__PURE__ */ new Date()).toISOString().slice(0, 7)) return true;
				return false;
			});
		});
		const todoMonthStats = computed(() => {
			const list = currentMonthTodos.value;
			const total = list.length;
			const pending = list.filter((t) => t.status === "Pending").length;
			const inProgress = list.filter((t) => t.status === "In Progress").length;
			const completed = list.filter((t) => t.status === "Completed").length;
			return {
				total,
				pending,
				inProgress,
				completed,
				completedPct: total ? Math.round(completed / total * 100) : 0
			};
		});
		computed(() => {
			const [yearStr, monthStr] = month.value.split("-");
			const y = parseInt(yearStr, 10);
			const m = parseInt(monthStr, 10);
			const totalDays = new Date(y, m, 0).getDate();
			const firstDayIndex = new Date(y, m - 1, 1).getDay();
			const days = [];
			for (let i = 0; i < firstDayIndex; i++) days.push({
				dayNum: null,
				dateStr: null,
				hasTasks: false,
				count: 0
			});
			for (let d = 1; d <= totalDays; d++) {
				const dStr = `${month.value}-${String(d).padStart(2, "0")}`;
				const dayTasks = data.value.todos.filter((t) => t.due === dStr || t.createdDate === dStr);
				const count = dayTasks.length;
				let statusTone = void 0;
				if (count > 0) {
					const allCompleted = dayTasks.every((t) => t.status === "Completed");
					const hasInProgress = dayTasks.some((t) => t.status === "In Progress");
					if (allCompleted) statusTone = "completed";
					else if (hasInProgress) statusTone = "in-progress";
					else statusTone = "pending";
				}
				days.push({
					dayNum: d,
					dateStr: dStr,
					hasTasks: count > 0,
					statusTone,
					count
				});
			}
			return days;
		});
		const filteredKanbanTodos = computed(() => {
			if (todoSelectedDay.value) return data.value.todos.filter((t) => t.due === todoSelectedDay.value || t.createdDate === todoSelectedDay.value);
			return currentMonthTodos.value;
		});
		const expandedTodoIds = ref({});
		const isTodoExpanded = (id) => !!expandedTodoIds.value[id];
		computed(() => {
			if (!filteredKanbanTodos.value.length) return false;
			return filteredKanbanTodos.value.every((t) => expandedTodoIds.value[t.id]);
		});
		ref("");
		const exportContext = ref("timesheets");
		const exportDataType = ref("timesheets");
		const exportDateRange = ref("current_month");
		const exportFileFormat = ref("csv");
		const exportCustomStartDate = ref("");
		const exportCustomEndDate = ref("");
		const computedExportRange = computed(() => {
			const [yStr, mStr] = (month.value || (/* @__PURE__ */ new Date()).toISOString().slice(0, 7)).split("-");
			const y = parseInt(yStr, 10) || (/* @__PURE__ */ new Date()).getFullYear();
			const m = parseInt(mStr, 10) || (/* @__PURE__ */ new Date()).getMonth() + 1;
			if (exportDateRange.value === "current_month") {
				const start = `${y}-${String(m).padStart(2, "0")}-01`;
				const lastDay = new Date(y, m, 0).getDate();
				return {
					start,
					end: `${y}-${String(m).padStart(2, "0")}-${String(lastDay).padStart(2, "0")}`,
					label: "Current Month",
					displayLabel: `${monthNamesShort[m - 1] || "Current Month"} ${y}`
				};
			}
			if (exportDateRange.value === "last_3_months") {
				const startObj = new Date(y, m - 3, 1);
				const endObj = new Date(y, m, 0);
				const startY = startObj.getFullYear();
				const start = `${startY}-${String(startObj.getMonth() + 1).padStart(2, "0")}-01`;
				const endY = endObj.getFullYear();
				return {
					start,
					end: `${endY}-${String(endObj.getMonth() + 1).padStart(2, "0")}-${String(endObj.getDate()).padStart(2, "0")}`,
					label: "Last 3 Months",
					displayLabel: `${monthNamesShort[startObj.getMonth()]} ${startY} - ${monthNamesShort[endObj.getMonth()]} ${endY}`
				};
			}
			if (exportDateRange.value === "last_6_months") {
				const startObj = new Date(y, m - 6, 1);
				const endObj = new Date(y, m, 0);
				const startY = startObj.getFullYear();
				const start = `${startY}-${String(startObj.getMonth() + 1).padStart(2, "0")}-01`;
				const endY = endObj.getFullYear();
				return {
					start,
					end: `${endY}-${String(endObj.getMonth() + 1).padStart(2, "0")}-${String(endObj.getDate()).padStart(2, "0")}`,
					label: "Last 6 Months",
					displayLabel: `${monthNamesShort[startObj.getMonth()]} ${startY} - ${monthNamesShort[endObj.getMonth()]} ${endY}`
				};
			}
			const start = exportCustomStartDate.value || "2000-01-01";
			const end = exportCustomEndDate.value || "2099-12-31";
			return {
				start,
				end,
				label: "Custom Range",
				displayLabel: `${formatDateDMY(start)} to ${formatDateDMY(end)}`
			};
		});
		const allUnifiedTimesheets = computed(() => {
			const logRecords = (data.value.logs || []).map((item) => {
				const rawDate = item.date || "";
				const proj = item.project || (item.text ? item.text.split(" · ")[2] : "") || "Project Alpha";
				const tName = item.task || (item.text ? item.text.split(" · ")[0] : "") || "Work session";
				const team = item.users && item.users.length ? item.users : item.user ? [item.user] : ["TEZ"];
				return {
					id: item.id,
					date: rawDate,
					project: proj,
					task: tName,
					tags: item.tags || [],
					team,
					hours: Number(item.hours || 0),
					startTime: item.startTime || "",
					endTime: item.endTime || "",
					status: "Completed",
					priority: "Medium"
				};
			});
			if (logRecords.length > 0) return logRecords;
			return (data.value.tasks || []).map((t) => {
				const rawDate = t.startDate || (t.month ? `${t.month}-01` : "");
				const team = t.owners && t.owners.length ? t.owners : t.owner ? [t.owner] : ["TEZ"];
				return {
					id: t.id,
					date: rawDate,
					project: t.project || "Project Alpha",
					task: t.task || "Work entry",
					tags: t.tags || [],
					team,
					hours: Number(t.hours || 0),
					startTime: t.startTime || "",
					endTime: t.endTime || "",
					status: t.status || "In Progress",
					priority: t.priority || "Medium"
				};
			});
		});
		const filteredExportTimesheets = computed(() => {
			if (exportOnlySelected.value && selectedLogIds.value.length > 0) return allUnifiedTimesheets.value.filter((item) => selectedLogIds.value.includes(item.id));
			const { start, end } = computedExportRange.value;
			return allUnifiedTimesheets.value.filter((item) => {
				if (!item.date) return false;
				const d = item.date.length === 7 ? `${item.date}-01` : item.date;
				return d >= start && d <= end;
			}).sort((a, b) => b.date.localeCompare(a.date));
		});
		const filteredExportTodos = computed(() => {
			const { start, end } = computedExportRange.value;
			return (data.value.todos || []).filter((td) => {
				const d = td.createdDate || td.due || (td.month ? `${td.month}-01` : "");
				if (!d) return true;
				const compDate = d.length === 7 ? `${d}-01` : d;
				return compDate >= start && compDate <= end;
			}).sort((a, b) => {
				const da = a.createdDate || a.due || "";
				return (b.createdDate || b.due || "").localeCompare(da);
			});
		});
		return (_ctx, _push, _parent, _attrs) => {
			_push(`<div${ssrRenderAttrs(mergeProps({ class: ["app-shell", [{ dark: unref(dark) }, { "sidebar-collapsed": unref(collapsed) }]] }, _attrs))}><input type="file" accept="*/*" hidden><datalist id="project-list"><!--[-->`);
			ssrRenderList(unref(data).projects, (p) => {
				_push(`<option${ssrRenderAttr("value", p)}>${ssrInterpolate(p)}</option>`);
			});
			_push(`<!--]--></datalist><datalist id="tag-list"><!--[-->`);
			ssrRenderList(predefinedTags, (t) => {
				_push(`<option${ssrRenderAttr("value", t)}>${ssrInterpolate(t)}</option>`);
			});
			_push(`<!--]--></datalist><datalist id="user-list"><!--[-->`);
			ssrRenderList(unref(data).users, (u) => {
				_push(`<option${ssrRenderAttr("value", u)}>${ssrInterpolate(u)}</option>`);
			});
			_push(`<!--]--></datalist><aside class="sidebar"><div class="logo"><span>MT</span><div><b>my tracker</b><small>work ecosystem</small></div></div><p class="nav-label">WORKSPACE</p><nav><!--[-->`);
			ssrRenderList([
				{
					id: "dashboard",
					label: "Overview",
					icon: "⌂"
				},
				{
					id: "logs",
					label: "My timesheets / logs",
					icon: "▤"
				},
				{
					id: "todos",
					label: "To-dos",
					icon: "✓"
				},
				{
					id: "calendar",
					label: "Calendar",
					icon: "▦"
				},
				{
					id: "meetings",
					label: "Meetings",
					icon: "◫"
				},
				{
					id: "feedback",
					label: "Feedback / notes",
					icon: "✦"
				},
				{
					id: "reports",
					label: "Reports",
					icon: "▥"
				},
				{
					id: "integrations",
					label: "Integrations",
					icon: "◎"
				}
			], (item) => {
				_push(`<button class="${ssrRenderClass({ active: unref(screen) === item.id })}">${ssrInterpolate(item.icon)} <span>${ssrInterpolate(item.label)}</span></button>`);
			});
			_push(`<!--]--></nav><div class="sidebar-bottom"> Saved locally<br><small>Google Calendar · Outlook ready</small></div></aside><section class="app-main"><header class="topbar"><button class="collapse-button">☰</button><div class="top-search"> ⌕ <input${ssrRenderAttr("value", unref(search))} placeholder="Search workspace, tasks, logs..."></div><div class="top-actions"><div class="add-new-dropdown-wrapper"><button class="btn-add-new-dropdown"> ＋ Add new ▾ </button>`);
			if (unref(addNewOpen)) _push(`<div class="add-new-menu"><button> ✎ Add manually </button><button> ⏱ Start timer </button><button> ⇧ Import / Upload file </button></div>`);
			else _push(`<!---->`);
			_push(`</div><div class="notif-wrapper"><button class="theme-toggle" title="Notifications"><svg width="17" height="17" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M18 8A6 6 0 0 0 6 8c0 7-3 9-3 9h18s-3-2-3-9"></path><path d="M13.73 21a2 2 0 0 1-3.46 0"></path></svg></button>`);
			if (unref(data).notifications.some((n) => !n.read)) _push(`<span class="notif-badge"></span>`);
			else _push(`<!---->`);
			if (unref(notifOpen)) {
				_push(`<div class="notif-popover"><div class="notif-popover-head"><h4>Notifications</h4><div><button>Mark read</button> · <button>Clear</button></div></div><div class="notif-list"><!--[-->`);
				ssrRenderList(unref(data).notifications, (n) => {
					_push(`<div class="${ssrRenderClass([{ unread: !n.read }, "notif-item"])}"><span class="${ssrRenderClass([n.type, "notif-icon"])}">${ssrInterpolate(n.type === "success" ? "✓" : n.type === "warning" ? "!" : "i")}</span><div class="notif-content"><b>${ssrInterpolate(n.title)}</b><p>${ssrInterpolate(n.message)}</p><small>${ssrInterpolate(n.time)}</small></div></div>`);
				});
				_push(`<!--]-->`);
				if (!unref(data).notifications.length) _push(`<div class="empty" style="${ssrRenderStyle({ "padding": "20px" })}"> No notifications </div>`);
				else _push(`<!---->`);
				_push(`</div></div>`);
			} else _push(`<!---->`);
			_push(`</div><button class="theme-toggle" title="Toggle theme">◐</button><button class="avatar">TEZ</button>`);
			if (unref(profileOpen)) _push(`<div class="profile-menu"><b>TEZ</b><small>tez@mytracker.local</small><button>View profile</button><button>Log out</button></div>`);
			else _push(`<!---->`);
			_push(`</div></header>`);
			if (unref(timer) && unref(screen) !== "logs") _push(`<div class="global-timer-ribbon"><div class="timer-ribbon-content"><div class="timer-ribbon-left"><span class="ribbon-pulse-badge"><span class="ribbon-pulse-dot"></span> LIVE TIMER ACTIVE </span><div class="ribbon-task-details"><span class="ribbon-project-tag">📁 ${ssrInterpolate(unref(timerProject) || "General")}</span><span class="ribbon-divider">·</span><strong class="ribbon-task-title">${ssrInterpolate(unref(timerTask) || "Active Work Session")}</strong></div></div><div class="timer-ribbon-right"><div class="ribbon-time-display"><span class="ribbon-time-icon">⏱</span><strong class="ribbon-time-digits">${ssrInterpolate(Math.floor(unref(elapsed) / 3600).toString().padStart(2, "0"))}:${ssrInterpolate(Math.floor(unref(elapsed) / 60 % 60).toString().padStart(2, "0"))}:${ssrInterpolate((unref(elapsed) % 60).toString().padStart(2, "0"))}</strong></div><button type="button" class="ribbon-stop-btn" title="Stop live session and save work log"> ⏹ End &amp; Log Session </button></div></div></div>`);
			else _push(`<!---->`);
			_push(`<main class="content">`);
			if (unref(screen) === "dashboard") {
				_push(`<section class="screen"><div class="page-head"><div><p class="eyebrow">${ssrInterpolate((/* @__PURE__ */ new Date()).toLocaleDateString("en-US", {
					weekday: "long",
					day: "numeric",
					month: "long",
					year: "numeric"
				}).toUpperCase())}</p><h1>${ssrInterpolate(unref(greeting))}, TEZ<span>.</span></h1><p class="muted">Your work analytics, action items, and timesheets in one place.</p></div><div style="${ssrRenderStyle({
					"display": "flex",
					"align-items": "center",
					"gap": "12px"
				})}"><button class="button primary" style="${ssrRenderStyle({
					"display": "flex",
					"align-items": "center",
					"gap": "6px"
				})}"> ＋ Add log </button><div class="month-select"><button>‹</button><b>${ssrInterpolate(unref(monthLabel))}</b><button>›</button><button class="btn-month-cal" title="Open month picker calendar"> 📅 </button>`);
				if (unref(showMonthPicker)) {
					_push(`<div class="month-picker-popover"><div class="month-picker-header"><button>‹</button><span>${ssrInterpolate(unref(pickerYear))}</span><button>›</button></div><div class="month-grid"><!--[-->`);
					ssrRenderList(monthNamesShort, (mName, idx) => {
						_push(`<button class="${ssrRenderClass({ active: unref(month) === `${unref(pickerYear)}-${String(idx + 1).padStart(2, "0")}` })}">${ssrInterpolate(mName)}</button>`);
					});
					_push(`<!--]--></div></div>`);
				} else _push(`<!---->`);
				_push(`</div></div></div><div class="hero-grid"><article class="focus-card"><div><p class="eyebrow">CURRENT FOCUS</p><h2>${ssrInterpolate(unref(monthTasks)[0]?.task || "Complete project deliverable")}</h2><p class="muted">Keep the momentum moving.</p></div><button class="circle-arrow">↗</button></article><div class="stat-card"><label>HOURS LOGGED</label><strong>${ssrInterpolate(unref(hours).toFixed(1))}</strong><small>${ssrInterpolate(unref(monthLabel))}</small></div><div class="stat-card green"><label>DELIVERY RATE</label><strong>${ssrInterpolate(unref(monthTasks).length ? Math.round(unref(done) / unref(monthTasks).length * 100) : 0)}<small>%</small></strong><small>${ssrInterpolate(unref(done))} of ${ssrInterpolate(unref(monthTasks).length)} tasks complete</small></div></div><div class="dashboard-grid"><section class="panel analytics-card"><div class="chart-header-row"><div><h3>Work analytics</h3><p class="muted" style="${ssrRenderStyle({ "font-size": "11px" })}">${ssrInterpolate(unref(chartRange) === "month" ? "Annual trajectory (12-month area trend)" : unref(chartRange) === "week" ? "Weekly breakdown across 7 days" : "Hours logged by day (scroll horizontally for all dates)")}</p></div><div class="chart-range-tabs"><button class="${ssrRenderClass({ active: unref(chartRange) === "day" })}">Day</button><button class="${ssrRenderClass({ active: unref(chartRange) === "week" })}">Week</button><button class="${ssrRenderClass({ active: unref(chartRange) === "month" })}">Month</button></div></div><div class="chart-wrapper"><div class="y-axis"><span>${ssrInterpolate(unref(maxChartHours))}h</span><span>${ssrInterpolate(Math.round(unref(maxChartHours) * .75))}h</span><span>${ssrInterpolate(Math.round(unref(maxChartHours) * .5))}h</span><span>${ssrInterpolate(Math.round(unref(maxChartHours) * .25))}h</span><span>0h</span></div>`);
				if (unref(chartRange) === "month") {
					_push(`<div class="month-line-chart-wrapper"><svg class="month-line-svg" viewBox="0 0 1000 210" preserveAspectRatio="none"><defs><linearGradient id="monthGradient" x1="0%" y1="0%" x2="0%" y2="100%"><stop offset="0%" stop-color="#3569df" stop-opacity="0.38"></stop><stop offset="65%" stop-color="#3569df" stop-opacity="0.08"></stop><stop offset="100%" stop-color="#3569df" stop-opacity="0.0"></stop></linearGradient></defs><line x1="20" y1="30" x2="980" y2="30" class="chart-guide-line"></line><line x1="20" y1="70" x2="980" y2="70" class="chart-guide-line"></line><line x1="20" y1="110" x2="980" y2="110" class="chart-guide-line"></line><line x1="20" y1="150" x2="980" y2="150" class="chart-guide-line"></line><line x1="20" y1="195" x2="980" y2="195" class="chart-axis-line"></line><path${ssrRenderAttr("d", unref(monthChartSvgPath).area)} fill="url(#monthGradient)"></path><path${ssrRenderAttr("d", unref(monthChartSvgPath).line)} fill="none" stroke="#3569df" stroke-width="3.5" stroke-linecap="round" stroke-linejoin="round"></path><!--[-->`);
					ssrRenderList(unref(monthChartPoints), (pt, idx) => {
						_push(`<g class="month-dot-group"><circle${ssrRenderAttr("cx", pt.x)}${ssrRenderAttr("cy", pt.y)} r="11" class="month-dot-halo"></circle><circle${ssrRenderAttr("cx", pt.x)}${ssrRenderAttr("cy", pt.y)} r="4.5" class="${ssrRenderClass([{ active: pt.monthKey === unref(month) }, "month-dot"])}"></circle></g>`);
					});
					_push(`<!--]--></svg><div class="month-dots-overlay"><!--[-->`);
					ssrRenderList(unref(monthChartPoints), (pt, idx) => {
						_push(`<div class="month-point-hotspot" style="${ssrRenderStyle({ left: `${idx / 11 * 100}%` })}"><div class="month-hover-tooltip"><b>${ssrInterpolate(pt.hours)} hrs</b><small>${ssrInterpolate(pt.label)}</small></div></div>`);
					});
					_push(`<!--]--></div><div class="month-x-labels-row"><!--[-->`);
					ssrRenderList(unref(monthChartPoints), (pt, idx) => {
						_push(`<span class="${ssrRenderClass({ active: pt.monthKey === unref(month) })}">${ssrInterpolate(pt.label)}</span>`);
					});
					_push(`<!--]--></div></div>`);
				} else if (unref(chartRange) === "week") {
					_push(`<div class="week-bars-wrapper"><div class="week-svg-container"><div class="chart-grid-lines"><div class="grid-line" style="${ssrRenderStyle({ "top": "15%" })}"></div><div class="grid-line" style="${ssrRenderStyle({ "top": "40%" })}"></div><div class="grid-line" style="${ssrRenderStyle({ "top": "65%" })}"></div></div><div class="week-bar-group"><!--[-->`);
					ssrRenderList(unref(chartDataPoints), (point, idx) => {
						_push(`<div class="week-bar-col"><span class="chart-bar-tooltip">${ssrInterpolate(point.hours)} hrs (${ssrInterpolate(point.label)})</span><div class="chart-bar week-bar" style="${ssrRenderStyle({ height: Math.min(100, Math.max(6, point.hours / unref(maxChartHours) * 100)) + "%" })}"></div></div>`);
					});
					_push(`<!--]--></div></div><div class="week-x-labels"><!--[-->`);
					ssrRenderList(unref(chartDataPoints), (point, idx) => {
						_push(`<span>${ssrInterpolate(point.label)}</span>`);
					});
					_push(`<!--]--></div></div>`);
				} else {
					_push(`<div class="chart-body"><div class="chart-scroll-content"><div class="chart-svg-container"><div class="chart-bar-group"><!--[-->`);
					ssrRenderList(unref(chartDataPoints), (point, idx) => {
						_push(`<div class="chart-bar-col"><span class="chart-bar-tooltip">${ssrInterpolate(point.hours)} hrs (${ssrInterpolate(point.label)})</span><div class="chart-bar" style="${ssrRenderStyle({ height: Math.min(100, Math.max(8, point.hours / unref(maxChartHours) * 100)) + "%" })}"></div></div>`);
					});
					_push(`<!--]--></div></div><div class="x-axis-labels"><!--[-->`);
					ssrRenderList(unref(chartDataPoints), (point, idx) => {
						_push(`<span>${ssrInterpolate(point.label)}</span>`);
					});
					_push(`<!--]--></div></div></div>`);
				}
				_push(`</div></section><section class="panel todo-analytics-card"><div class="panel-head"><div><h3>To-do analytics &amp; Delivery trends</h3><p>Action items breakdown and completion trajectory</p></div><button class="text-button">View board →</button></div><div class="todo-trend-banner"><div class="todo-trend-left"><span>⚡ Action Item Completion Rate</span></div><span class="todo-trend-badge">${ssrInterpolate(unref(todoStats).completedPct)}% Delivered (+12% this week)</span></div><div class="todo-donut-container"><div class="donut-graphic"><svg viewBox="0 0 100 100" class="donut-svg"><circle cx="50" cy="50" r="40" fill="transparent" stroke="#edf1f5" stroke-width="16"></circle><circle cx="50" cy="50" r="40" fill="transparent" stroke="#ef9b27" stroke-width="16"${ssrRenderAttr("stroke-dasharray", unref(todoStats).pendingStroke)} stroke-dashoffset="0"></circle><circle cx="50" cy="50" r="40" fill="transparent" stroke="#3569df" stroke-width="16"${ssrRenderAttr("stroke-dasharray", unref(todoStats).inProgressStroke)}${ssrRenderAttr("stroke-dashoffset", unref(todoStats).inProgressOffset)}></circle><circle cx="50" cy="50" r="40" fill="transparent" stroke="#11a981" stroke-width="16"${ssrRenderAttr("stroke-dasharray", unref(todoStats).completedStroke)}${ssrRenderAttr("stroke-dashoffset", unref(todoStats).completedOffset)}></circle></svg><div class="donut-center-text"><strong>${ssrInterpolate(unref(todoStats).total)}</strong><small>To-dos</small></div></div><div class="todo-legend-list"><div class="todo-legend-item"><div class="todo-legend-left"><span class="todo-legend-dot pending"></span><span>Pending</span></div><span class="todo-legend-count">${ssrInterpolate(unref(todoStats).pending)} items (${ssrInterpolate(unref(todoStats).pendingPct)}%)</span></div><div class="todo-legend-item"><div class="todo-legend-left"><span class="todo-legend-dot in-progress"></span><span>In Progress</span></div><span class="todo-legend-count">${ssrInterpolate(unref(todoStats).inProgress)} items (${ssrInterpolate(unref(todoStats).inProgressPct)}%)</span></div><div class="todo-legend-item"><div class="todo-legend-left"><span class="todo-legend-dot completed"></span><span>Completed</span></div><span class="todo-legend-count">${ssrInterpolate(unref(todoStats).completed)} items (${ssrInterpolate(unref(todoStats).completedPct)}%)</span></div></div></div><button class="wide-link">＋ Add action item</button></section></div></section>`);
			} else if (unref(screen) === "logs") {
				_push(`<section class="screen"><div class="page-head"><div><p class="eyebrow">TIME CONTROL</p><h1>My timesheets / logs</h1><p class="muted">Live session tracking and timeline of completed work.</p></div><div class="page-head-actions" style="${ssrRenderStyle({
					"display": "flex",
					"align-items": "center",
					"gap": "10px",
					"flex-wrap": "wrap"
				})}"><div class="month-select"><button>‹</button><b>${ssrInterpolate(unref(monthLabel))}</b><button>›</button><button class="btn-month-cal" title="Open month picker calendar"> 📅 </button>`);
				if (unref(showLogsMonthPicker)) {
					_push(`<div class="month-picker-popover"><div class="month-picker-header"><button>‹</button><span>${ssrInterpolate(unref(pickerYear))}</span><button>›</button></div><div class="month-grid"><!--[-->`);
					ssrRenderList(monthNamesShort, (mName, idx) => {
						_push(`<button class="${ssrRenderClass({ active: unref(month) === `${unref(pickerYear)}-${String(idx + 1).padStart(2, "0")}` })}">${ssrInterpolate(mName)}</button>`);
					});
					_push(`<!--]--></div></div>`);
				} else _push(`<!---->`);
				_push(`</div><button class="${ssrRenderClass([{ "primary-outlined": unref(selectedLogIds).length > 0 }, "button"])}" style="${ssrRenderStyle({
					"display": "inline-flex",
					"align-items": "center",
					"gap": "6px",
					"padding": "8px 14px",
					"font-weight": "600"
				})}"><span>⇩</span><span>${ssrInterpolate(unref(selectedLogIds).length > 0 ? `Export (${unref(selectedLogIds).length})` : "Export")}</span></button><button class="button secondary-btn" style="${ssrRenderStyle({
					"display": "inline-flex",
					"align-items": "center",
					"gap": "6px",
					"padding": "8px 14px",
					"font-weight": "600"
				})}"${ssrRenderAttr("title", unref(data).officeIntegration?.enabled ? "Sync logs bi-directionally with Office Timesheets" : "Office Timesheets integration inactive")}><span class="${ssrRenderClass({ "spin-icon": unref(isSyncingOffice) })}">🔄</span><span>${ssrInterpolate(unref(isSyncingOffice) ? "Syncing..." : "Sync Office")}</span></button>`);
				if (unref(selectedLogIds).length > 0) _push(`<button class="button danger-btn" style="${ssrRenderStyle({
					"display": "inline-flex",
					"align-items": "center",
					"gap": "6px",
					"padding": "8px 14px",
					"font-weight": "600",
					"background": "#dc2626",
					"color": "#ffffff",
					"border-color": "#dc2626"
				})}" title="Delete selected log entries"><span>🗑</span><span>Delete (${ssrInterpolate(unref(selectedLogIds).length)})</span></button>`);
				else _push(`<!---->`);
				if (unref(selectedLogIds).length > 0) _push(`<button class="button danger-btn" style="${ssrRenderStyle({
					"display": "inline-flex",
					"align-items": "center",
					"gap": "6px",
					"padding": "8px 14px",
					"font-weight": "600",
					"background": "#dc2626",
					"color": "#ffffff",
					"border-color": "#dc2626"
				})}" title="Delete selected log entries"><span>🗑</span><span>Delete (${ssrInterpolate(unref(selectedLogIds).length)})</span></button>`);
				else _push(`<!---->`);
				if (unref(selectedLogIds).length > 0) _push(`<button class="button" style="${ssrRenderStyle({
					"font-size": "12px",
					"padding": "8px 10px"
				})}" title="Clear selection"> ✕ Clear </button>`);
				else _push(`<!---->`);
				_push(`<button class="button primary">＋ New log</button></div></div><div class="timer-strip-box"><div class="timer-fields-row"><div class="timer-field-group project-group"><label class="timer-field-label">SELECT PROJECT</label><div class="timer-input-wrap"><span class="timer-input-icon">📁</span><select class="timer-project-select"><option value="" disabled${ssrIncludeBooleanAttr(Array.isArray(unref(timerProject)) ? ssrLooseContain(unref(timerProject), "") : ssrLooseEqual(unref(timerProject), "")) ? " selected" : ""}>Select Project...</option><!--[-->`);
				ssrRenderList(unref(data).projects, (p) => {
					_push(`<option${ssrRenderAttr("value", p)}${ssrIncludeBooleanAttr(Array.isArray(unref(timerProject)) ? ssrLooseContain(unref(timerProject), p) : ssrLooseEqual(unref(timerProject), p)) ? " selected" : ""}>${ssrInterpolate(p)}</option>`);
				});
				_push(`<!--]--></select></div></div><div class="timer-field-group task-group"><label class="timer-field-label">TASK NAME</label><div class="timer-input-wrap"><span class="timer-input-icon">⚡</span><input${ssrRenderAttr("value", unref(timerTask))} class="timer-task-input" placeholder="What are you working on? (e.g. Navigation UI Refactor)"></div></div></div><div class="timer-action-cluster"><div class="timer-counter-wrapper"><div class="timer-digits">${ssrInterpolate(Math.floor(unref(elapsed) / 3600).toString().padStart(2, "0"))}:${ssrInterpolate(Math.floor(unref(elapsed) / 60 % 60).toString().padStart(2, "0"))}:${ssrInterpolate((unref(elapsed) % 60).toString().padStart(2, "0"))}</div>`);
				if (unref(timer)) _push(`<div class="timer-live-badge"><span class="live-pulse-dot"></span> LIVE </div>`);
				else _push(`<!---->`);
				_push(`</div><button class="${ssrRenderClass([{ running: unref(timer) }, "timer-toggle-btn"])}">${ssrInterpolate(unref(timer) ? "⏹ Stop & Log" : "▶ Start timer")}</button></div></div><div class="date-grouped-logs-container"><!--[-->`);
				ssrRenderList(unref(groupedLogsByDate), (group) => {
					_push(`<div class="date-log-group-card"><div class="date-group-header"><div class="date-group-title-side"><span class="${ssrRenderClass([{ "is-today": group.isToday }, "date-group-label"])}">${ssrInterpolate(group.relativeLabel)}</span><span class="${ssrRenderClass([{ "has-selected": getGroupSelectedCount(group) > 0 }, "date-entries-count"])}">${ssrInterpolate(getGroupSelectedCount(group) > 0 ? `${getGroupSelectedCount(group)}/${group.items.length} selected` : `${group.items.length} ${group.items.length === 1 ? "entry" : "entries"}`)}</span></div><div class="date-group-summary-side"><span class="day-total-badge"><small>DAY TOTAL:</small><b>${ssrInterpolate(group.formattedTotalDuration)}</b><span class="day-total-dec">(${ssrInterpolate(group.totalHours.toFixed(1))}h)</span></span></div></div><div class="${ssrRenderClass([{ "selection-active": unref(selectedLogIds).length > 0 }, "panel table-panel day-table-panel"])}"><table><thead><tr><th class="select-col" style="${ssrRenderStyle({
						"width": "38px",
						"text-align": "center"
					})}"><input type="checkbox" class="custom-table-checkbox"${ssrIncludeBooleanAttr(isGroupAllSelected(group)) ? " checked" : ""} title="Select / deselect all logs for this day"></th><th style="${ssrRenderStyle({ "width": "20%" })}">Project Name</th><th style="${ssrRenderStyle({ "width": "34%" })}">Task Name</th><th style="${ssrRenderStyle({ "width": "17%" })}">Time / Hours</th><th style="${ssrRenderStyle({ "width": "17%" })}">Assign Team / Sync</th><th style="${ssrRenderStyle({
						"width": "12%",
						"text-align": "right"
					})}">`);
					if (!getGroupSelectedCount(group)) _push(`<span>Actions</span>`);
					else {
						_push(`<div class="group-bulk-menu-wrap inline-header-menu"><button type="button" class="btn-dots-menu sm"${ssrRenderAttr("title", `Actions menu for ${getGroupSelectedCount(group)} selected items`)}> ⋮ </button>`);
						if (unref(bulkMenuOpenGroupDate) === group.date + "-th") _push(`<div class="bulk-popover-menu right-aligned"><button type="button"><span>⇩</span> Export selected (${ssrInterpolate(getGroupSelectedCount(group))}) </button><button type="button" class="delete-opt"><span>🗑</span> Delete selected (${ssrInterpolate(getGroupSelectedCount(group))}) </button><button type="button" class="clear-opt"><span>✕</span> Clear selection </button></div>`);
						else _push(`<!---->`);
						_push(`</div>`);
					}
					_push(`</th></tr></thead><tbody><!--[-->`);
					ssrRenderList(group.items, (item) => {
						_push(`<tr class="${ssrRenderClass({ "row-selected": isLogSelected(item.id) })}"><td class="select-col" style="${ssrRenderStyle({
							"width": "38px",
							"text-align": "center"
						})}"><input type="checkbox" class="custom-table-checkbox"${ssrIncludeBooleanAttr(isLogSelected(item.id)) ? " checked" : ""}></td><td><div class="table-project-cell"><span class="project-pill-label">📁 ${ssrInterpolate(item.project || (item.text ? item.text.split(" · ")[2] : "Project Alpha"))}</span>`);
						if (item.tags && item.tags.length) {
							_push(`<div class="tag-badges-wrapper"><!--[-->`);
							ssrRenderList(item.tags, (tag) => {
								_push(`<span class="tag-badge-item">${ssrInterpolate(tag)}</span>`);
							});
							_push(`<!--]--></div>`);
						} else _push(`<!---->`);
						_push(`</div></td><td><b class="table-task-name">${ssrInterpolate(item.task || (item.text ? item.text.split(" · ")[0] : item.text))}</b></td><td><div class="${ssrRenderClass([{ "show-duration": isTimeToggled(item.id) }, "time-toggle-pill"])}" title="Click to toggle between time range (10:00 - 11:00) and duration"><span class="time-toggle-icon">${ssrInterpolate(isTimeToggled(item.id) ? "⏳" : "⏱")}</span><span class="time-toggle-text">${ssrInterpolate(isTimeToggled(item.id) ? formatDynamicDuration(item.hours) : getLogTimeRange(item))}</span><span class="time-toggle-hint">⇄</span></div></td><td><div class="team-sync-cell" style="${ssrRenderStyle({
							"display": "flex",
							"align-items": "center",
							"gap": "8px",
							"flex-wrap": "wrap"
						})}"><div class="user-avatars-group"${ssrRenderAttr("title", "Logged by: " + getLogUsers(item).join(", "))}><!--[-->`);
						ssrRenderList(getLogUsers(item).slice(0, 3), (u) => {
							_push(`<span class="avatar-badge" style="${ssrRenderStyle({
								backgroundColor: getAvatarColor(u),
								color: "#ffffff"
							})}">${ssrInterpolate(u.trim().charAt(0).toUpperCase())}</span>`);
						});
						_push(`<!--]-->`);
						if (getLogUsers(item).length > 3) _push(`<span class="avatar-badge overflow"${ssrRenderAttr("title", getLogUsers(item).slice(3).join(", "))}> +${ssrInterpolate(getLogUsers(item).length - 3)}</span>`);
						else _push(`<!---->`);
						_push(`</div>`);
						if (item.officeSynced) _push(`<span class="office-sync-pill synced" title="Bi-directionally synced with Office Timesheets"> 🏢 Synced </span>`);
						else _push(`<span class="office-sync-pill pending" title="Local entry - click to sync with Office Timesheets"> ⚡ Sync </span>`);
						_push(`</div></td><td><div class="log-actions" style="${ssrRenderStyle({ "justify-content": "flex-end" })}"><button class="btn-action-icon" title="Edit log">✎</button><button class="btn-action-icon delete" title="Delete log">🗑</button></div></td></tr>`);
					});
					_push(`<!--]--></tbody></table></div></div>`);
				});
				_push(`<!--]-->`);
				if (!unref(groupedLogsByDate).length) _push(`<div class="panel empty" style="${ssrRenderStyle({
					"padding": "48px 20px",
					"text-align": "center"
				})}"><p style="${ssrRenderStyle({
					"font-size": "16px",
					"font-weight": "600",
					"margin": "0 0 6px",
					"color": "var(--ink)"
				})}">No work logs captured yet</p><p class="muted" style="${ssrRenderStyle({ "margin": "0 0 16px" })}">Use &quot;+ New log&quot; or the live timer above to log your time entries.</p><button class="button primary">＋ New log</button></div>`);
				else _push(`<!---->`);
				_push(`</div></section>`);
			} else if (unref(screen) === "todos") {
				_push(`<section class="screen"><div class="page-head"><div><p class="eyebrow">PERSONAL DELIVERY</p><h1>To-do board</h1><p class="muted">Track, drag, and complete scheduled deliverables for ${ssrInterpolate(unref(monthLabel))}.</p></div><div class="page-head-actions" style="${ssrRenderStyle({
					"display": "flex",
					"align-items": "center",
					"gap": "10px",
					"flex-wrap": "wrap"
				})}"><div class="month-select"><button>‹</button><b>${ssrInterpolate(unref(monthLabel))}</b><button>›</button><button class="btn-month-cal" title="Open month picker calendar"> 📅 </button>`);
				if (unref(showTodoMonthPicker)) {
					_push(`<div class="month-picker-popover"><div class="month-picker-header"><button>‹</button><span>${ssrInterpolate(unref(pickerYear))}</span><button>›</button></div><div class="month-grid"><!--[-->`);
					ssrRenderList(monthNamesShort, (mName, idx) => {
						_push(`<button class="${ssrRenderClass({ active: unref(month) === `${unref(pickerYear)}-${String(idx + 1).padStart(2, "0")}` })}">${ssrInterpolate(mName)}</button>`);
					});
					_push(`<!--]--></div></div>`);
				} else _push(`<!---->`);
				_push(`</div><button class="button primary">＋ Add to-do</button></div></div><div class="todo-month-strip"><div class="todo-month-metrics"><div class="todo-metric-item"><label>Total Tasks</label><strong>${ssrInterpolate(unref(todoMonthStats).total)}</strong></div><div class="todo-metric-item"><label>Pending</label><strong class="amber">${ssrInterpolate(unref(todoMonthStats).pending)}</strong></div><div class="todo-metric-item"><label>In Progress</label><strong class="blue">${ssrInterpolate(unref(todoMonthStats).inProgress)}</strong></div><div class="todo-metric-item"><label>Completed</label><strong class="green">${ssrInterpolate(unref(todoMonthStats).completed)} <span class="${ssrRenderClass([{ green: unref(todoMonthStats).completedPct >= 70 }, "metric-pct-pill"])}">${ssrInterpolate(unref(todoMonthStats).completedPct)}% done </span></strong></div></div></div>`);
				if (!unref(currentMonthTodos).length && unref(data).todos.length) _push(`<div class="filter-active-bar" style="${ssrRenderStyle({
					"background": "#f8fafc",
					"border-color": "var(--line)",
					"color": "var(--muted)"
				})}"><span>No to-dos scheduled for <b>${ssrInterpolate(unref(monthLabel))}</b>. (${ssrInterpolate(unref(data).todos.length)} tasks exist across other months)</span><button type="button">Jump to Current Month</button></div>`);
				else _push(`<!---->`);
				_push(`<div class="kanban"><!--[-->`);
				ssrRenderList([
					"Pending",
					"In Progress",
					"Completed"
				], (status) => {
					_push(`<div class="kanban-col"><div class="column-title"><b>${ssrInterpolate(status)}</b><em>${ssrInterpolate(unref(filteredKanbanTodos).filter((t) => t.status === status).length)}</em></div><div class="kanban-cards-scroll"><!--[-->`);
					ssrRenderList(unref(filteredKanbanTodos).filter((t) => t.status === status), (item) => {
						_push(`<article class="${ssrRenderClass([{ "expanded": isTodoExpanded(item.id) }, "todo-card"])}" draggable="true"><div class="todo-card-header"><span class="${ssrRenderClass([tone(item.priority), "tag"])}">${ssrInterpolate(item.priority)}</span><div class="todo-card-actions">`);
						if (item.detail || item.link || item.fileName) _push(`<button type="button" class="btn-action-icon sm btn-toggle-expand"${ssrRenderAttr("title", isTodoExpanded(item.id) ? "Collapse details" : "Expand details")}><span class="${ssrRenderClass([{ "rotated": isTodoExpanded(item.id) }, "expand-icon"])}">▾</span></button>`);
						else _push(`<!---->`);
						_push(`<button type="button" class="btn-action-icon sm" title="Edit to-do">✎</button><button type="button" class="btn-action-icon sm delete" title="Delete to-do">🗑</button></div></div><h3 class="todo-title">${ssrInterpolate(item.title)}</h3>`);
						if (isTodoExpanded(item.id)) {
							_push(`<div class="todo-expanded-content">`);
							if (item.detail) _push(`<p class="todo-desc">${ssrInterpolate(item.detail)}</p>`);
							else _push(`<!---->`);
							if (item.link || item.fileName) {
								_push(`<div class="todo-attachments-row">`);
								if (item.link) _push(`<a${ssrRenderAttr("href", item.link.startsWith("http") ? item.link : "https://" + item.link)} target="_blank" rel="noopener noreferrer" class="todo-link-badge" title="Open reference link"> 🔗 <span>${ssrInterpolate(getDisplayUrl(item.link))}</span> ↗ </a>`);
								else _push(`<!---->`);
								if (item.fileName) _push(`<span class="todo-file-badge"${ssrRenderAttr("title", "Attached file: " + item.fileName)}> 📎 <span>${ssrInterpolate(item.fileName)}</span></span>`);
								else _push(`<!---->`);
								_push(`</div>`);
							} else _push(`<!---->`);
							if (!item.detail && !item.link && !item.fileName) _push(`<p class="todo-desc muted" style="${ssrRenderStyle({
								"font-style": "italic",
								"margin-bottom": "6px"
							})}"> No additional notes or attachments. </p>`);
							else _push(`<!---->`);
							_push(`</div>`);
						} else _push(`<!---->`);
						_push(`<div class="todo-card-footer"><small class="todo-due-date">◷ ${ssrInterpolate(item.due || "No due date")}</small><div class="todo-footer-indicators">`);
						if (!isTodoExpanded(item.id) && item.detail) _push(`<span class="todo-indicator-icon" title="Has description notes">📝</span>`);
						else _push(`<!---->`);
						if (!isTodoExpanded(item.id) && item.link) _push(`<span class="todo-indicator-icon" title="Has reference link">🔗</span>`);
						else _push(`<!---->`);
						if (!isTodoExpanded(item.id) && item.fileName) _push(`<span class="todo-indicator-icon" title="Has attached file">📎</span>`);
						else _push(`<!---->`);
						_push(`<span class="${ssrRenderClass([tone(item.status), "todo-status-dot"])}"${ssrRenderAttr("title", "Status: " + item.status)}></span></div></div></article>`);
					});
					_push(`<!--]-->`);
					if (!unref(filteredKanbanTodos).filter((t) => t.status === status).length) _push(`<div class="kanban-empty"> No ${ssrInterpolate(status.toLowerCase())} tasks </div>`);
					else _push(`<!---->`);
					_push(`</div><button class="column-add">＋ Add item</button></div>`);
				});
				_push(`<!--]--></div></section>`);
			} else if (unref(screen) === "calendar") {
				_push(`<section class="screen"><div class="page-head"><div><p class="eyebrow">SCHEDULE</p><h1>Calendar</h1><p class="muted">Select a month to review scheduled deliverables.</p></div><button class="button primary">＋ New event</button></div><section class="panel calendar-large"><div class="calendar-toolbar"><select><!--[-->`);
				ssrRenderList(unref(months), (m) => {
					_push(`<option${ssrRenderAttr("value", m)}${ssrIncludeBooleanAttr(Array.isArray(unref(month)) ? ssrLooseContain(unref(month), m) : ssrLooseEqual(unref(month), m)) ? " selected" : ""}>${ssrInterpolate((/* @__PURE__ */ new Date(`${m}-01`)).toLocaleDateString("en-US", {
						month: "long",
						year: "numeric"
					}))}</option>`);
				});
				_push(`<!--]--></select></div><div class="weekdays"><!--[-->`);
				ssrRenderList([
					"Sun",
					"Mon",
					"Tue",
					"Wed",
					"Thu",
					"Fri",
					"Sat"
				], (d) => {
					_push(`<b>${ssrInterpolate(d)}</b>`);
				});
				_push(`<!--]--></div><div class="calendar-grid"><!--[-->`);
				ssrRenderList(35, (day) => {
					_push(`<button><b>${ssrInterpolate(day <= 30 ? day : "")}</b></button>`);
				});
				_push(`<!--]--></div></section></section>`);
			} else if (unref(screen) === "reports") _push(`<section class="screen"><div class="page-head"><div><p class="eyebrow">INSIGHTS</p><h1>Reports</h1><p class="muted">Detailed summary of time spent across projects.</p></div><button class="button primary">⇩ Export report</button></div><div class="report-cards"><div class="report-number"><label>MONTH HOURS</label><strong>${ssrInterpolate(unref(hours).toFixed(1))}h</strong><p>${ssrInterpolate(unref(monthLabel))}</p></div><div class="report-number"><label>OVERALL HOURS</label><strong>${ssrInterpolate(unref(data).tasks.reduce((s, t) => s + Number(t.hours || 0), 0).toFixed(1))}h</strong><p>all projects</p></div><div class="report-number"><label>COMPLETION</label><strong>${ssrInterpolate(unref(done))}/${ssrInterpolate(unref(monthTasks).length)}</strong><p>tasks delivered</p></div></div></section>`);
			else if (unref(screen) === "integrations") {
				_push(`<section class="screen"><div class="page-head"><div><p class="eyebrow">WORKSPACE CONNECTIVITY &amp; DEVELOPER HUB</p><h1>Integrations &amp; API Management</h1><p class="muted">Manage connected accounts, app integrations, and developer API credentials in separate clean views.</p></div><div class="page-head-actions"><button class="button primary"> 🔑 Create New API Key </button></div></div><div class="integration-tabs"><button class="${ssrRenderClass([{ active: unref(activeIntegrationTab) === "workspaces" }, "integration-tab-btn"])}"> 🏢 Connected Workspaces <span class="tab-badge">${ssrInterpolate(unref(data).workspaceConnections?.length || 4)}</span></button><button class="${ssrRenderClass([{ active: unref(activeIntegrationTab) === "catalog" }, "integration-tab-btn"])}"> 🔌 App Integrations Catalog <span class="tab-badge">11</span></button><button class="${ssrRenderClass([{ active: unref(activeIntegrationTab) === "developer_api" }, "integration-tab-btn"])}"> 🔑 Developer APIs &amp; Webhooks <span class="tab-badge">${ssrInterpolate(unref(data).apiKeys?.length || 0)}</span></button></div>`);
				if (unref(activeIntegrationTab) === "workspaces") {
					_push(`<div class="connected-workspaces-section"><div class="panel-head" style="${ssrRenderStyle({ "margin-bottom": "16px" })}"><div><h3>Active Workspace Connections</h3><p class="muted">Manage connected accounts and live sync options for your organization&#39;s workspaces.</p></div></div><div class="connected-workspaces-grid"><!--[-->`);
					ssrRenderList(unref(data).workspaceConnections || [], (conn) => {
						_push(`<article class="workspace-conn-card"><div><div class="conn-head"><div style="${ssrRenderStyle({
							"display": "flex",
							"align-items": "center",
							"gap": "12px"
						})}"><div class="${ssrRenderClass([conn.type, "conn-icon-box"])}">${ssrInterpolate(conn.icon)}</div><div class="conn-title"><h4>${ssrInterpolate(conn.name)}</h4><p class="conn-subtitle">${ssrInterpolate(conn.accountOrWorkspace)}</p></div></div><span class="${ssrRenderClass([conn.status, "status-pill-badge"])}"> ● ${ssrInterpolate(conn.status === "connected" ? "Active" : "Disconnected")}</span></div><p style="${ssrRenderStyle({
							"font-size": "12px",
							"color": "var(--muted)",
							"margin-bottom": "14px"
						})}">`);
						if (conn.type === "officetimesheets") _push(`<span>Bi-directional enterprise timesheets sync active.</span>`);
						else if (conn.type === "figma") _push(`<span>Tracking UI frame design hours and system components.</span>`);
						else if (conn.type === "mcp") _push(`<span>Streaming AI coding agent context and prompt logs into work logs.</span>`);
						else if (conn.type === "notion") _push(`<span>Syncing meeting notes and task databases automatically.</span>`);
						else _push(`<span>Connected workspace integration active.</span>`);
						_push(`</p></div><div class="conn-meta"><div><small class="muted" style="${ssrRenderStyle({ "display": "block" })}">Last Synced</small><b>${ssrInterpolate(conn.lastSynced)}</b></div><div style="${ssrRenderStyle({
							"display": "flex",
							"align-items": "center",
							"gap": "8px"
						})}"><button class="button secondary-btn sm"> Configure </button><button class="button primary sm"> Sync Now </button></div></div></article>`);
					});
					_push(`<!--]--></div><section class="panel integration-featured-card" style="${ssrRenderStyle({
						"padding": "24px",
						"margin-top": "12px"
					})}"><div class="featured-card-head" style="${ssrRenderStyle({
						"display": "flex",
						"align-items": "flex-start",
						"justify-content": "space-between",
						"gap": "16px",
						"margin-bottom": "20px",
						"flex-wrap": "wrap"
					})}"><div style="${ssrRenderStyle({
						"display": "flex",
						"align-items": "center",
						"gap": "14px"
					})}"><div class="conn-icon-box office" style="${ssrRenderStyle({
						"width": "48px",
						"height": "48px",
						"font-size": "24px"
					})}"> 🏢 </div><div><h3 style="${ssrRenderStyle({
						"margin": "0 0 4px",
						"font-size": "17px",
						"display": "flex",
						"align-items": "center",
						"gap": "10px"
					})}"> Office Timesheets Bi-Directional Endpoint Config <span class="${ssrRenderClass([unref(data).officeIntegration?.status || "connected", "status-pill-badge"])}"> ● ${ssrInterpolate(unref(data).officeIntegration?.status === "connected" ? "Connected & Active" : "Disconnected")}</span></h3><p class="muted" style="${ssrRenderStyle({
						"margin": "0",
						"font-size": "12px"
					})}"> Endpoint settings for Office Timesheets v4 API synchronization. </p></div></div><div style="${ssrRenderStyle({
						"display": "flex",
						"align-items": "center",
						"gap": "10px"
					})}"><button type="button" class="button secondary-btn sm"${ssrIncludeBooleanAttr(unref(isTestingConnection)) ? " disabled" : ""}><span class="${ssrRenderClass({ "spin-icon": unref(isTestingConnection) })}">🔌</span> ${ssrInterpolate(unref(isTestingConnection) ? "Testing..." : "Test Connection")}</button><button type="button" class="button primary sm"${ssrIncludeBooleanAttr(unref(isSyncingOffice)) ? " disabled" : ""}><span class="${ssrRenderClass({ "spin-icon": unref(isSyncingOffice) })}">🔄</span> ${ssrInterpolate(unref(isSyncingOffice) ? "Syncing Now..." : "Sync Now")}</button></div></div><form class="integration-form-grid" style="${ssrRenderStyle({
						"display": "grid",
						"grid-template-columns": "repeat(auto-fit, minmax(280px, 1fr))",
						"gap": "16px",
						"background": "var(--bg-subtle, #f8fafc)",
						"padding": "16px",
						"border-radius": "10px",
						"border": "1px solid var(--line, #e2e8f0)"
					})}"><div><label class="form-label" style="${ssrRenderStyle({ "margin-top": "0" })}">API Endpoint URL</label><input${ssrRenderAttr("value", unref(officeForm).endpoint)} type="url" required placeholder="https://api.officetimesheets.com/v1/sync"></div><div><label class="form-label" style="${ssrRenderStyle({
						"margin-top": "0",
						"display": "flex",
						"align-items": "center",
						"justify-content": "space-between"
					})}"><span>Office API Secret Key</span><button type="button" class="text-link" style="${ssrRenderStyle({ "font-size": "11px" })}">${ssrInterpolate(unref(showOfficeApiKey) ? "Hide" : "Show Secret")}</button></label><input${ssrRenderDynamicModel(unref(showOfficeApiKey) ? "text" : "password", unref(officeForm).apiKey, null)}${ssrRenderAttr("type", unref(showOfficeApiKey) ? "text" : "password")} required placeholder="Enter secret key..."></div><div style="${ssrRenderStyle({
						"grid-column": "1 / -1",
						"display": "flex",
						"align-items": "center",
						"justify-content": "space-between",
						"flex-wrap": "wrap",
						"gap": "12px",
						"padding-top": "8px",
						"border-top": "1px dashed var(--line, #cbd5e1)"
					})}"><div style="${ssrRenderStyle({
						"display": "flex",
						"align-items": "center",
						"gap": "20px",
						"flex-wrap": "wrap"
					})}"><label class="custom-toggle-label" style="${ssrRenderStyle({
						"display": "flex",
						"align-items": "center",
						"gap": "8px",
						"cursor": "pointer",
						"font-weight": "600",
						"font-size": "13px"
					})}"><input type="checkbox"${ssrIncludeBooleanAttr(Array.isArray(unref(officeForm).enabled) ? ssrLooseContain(unref(officeForm).enabled, null) : unref(officeForm).enabled) ? " checked" : ""}><span>Enable Integration</span></label><label class="custom-toggle-label" style="${ssrRenderStyle({
						"display": "flex",
						"align-items": "center",
						"gap": "8px",
						"cursor": "pointer",
						"font-weight": "600",
						"font-size": "13px"
					})}"><input type="checkbox"${ssrIncludeBooleanAttr(Array.isArray(unref(officeForm).autoSync) ? ssrLooseContain(unref(officeForm).autoSync, null) : unref(officeForm).autoSync) ? " checked" : ""}><span>Auto-Sync Work Logs</span></label></div><button type="submit" class="button primary sm">Save Settings</button></div></form></section></div>`);
				} else if (unref(activeIntegrationTab) === "catalog") _push(`<div class="app-catalog-section"><div class="panel-head" style="${ssrRenderStyle({ "margin-bottom": "16px" })}"><div><h3>App Integrations Catalog</h3><p class="muted">Connect design tools, AI context protocols, project management apps, and communication channels.</p></div></div><div class="integration-grid"><article class="integration-card"><div><div class="integration-card-top"><div class="integration-card-icon figma">🎨</div><div><h3>Figma Integration</h3><span class="category-pill">Design &amp; UI</span></div></div><p>Sync Figma frame edit time, design system components, and team activity logs directly into your daily work log.</p></div><div class="integration-card-foot"><span class="status-pill-badge connected">● Connected</span><button class="button primary sm">Configure</button></div></article><article class="integration-card"><div><div class="integration-card-top"><div class="integration-card-icon mcp">🤖</div><div><h3>MCP AI Context Protocol</h3><span class="category-pill">AI Agents &amp; Context</span></div></div><p>Connect Model Context Protocol (MCP) server nodes (Cursor, Antigravity, Claude) to automatically capture coding context &amp; log session hours.</p></div><div class="integration-card-foot"><span class="status-pill-badge connected">● Connected</span><button class="button primary sm">Configure</button></div></article><article class="integration-card"><div><div class="integration-card-top"><div class="integration-card-icon notion">📝</div><div><h3>Notion Workspace</h3><span class="category-pill">Docs &amp; Knowledge</span></div></div><p>Link Notion databases and workspace pages to automatically pull meeting notes, project specs, and deliverable checklists.</p></div><div class="integration-card-foot"><span class="status-pill-badge connected">● Connected</span><button class="button primary sm">Configure</button></div></article><article class="integration-card"><div><div class="integration-card-top"><div class="integration-card-icon office">🏢</div><div><h3>Office Timesheets</h3><span class="category-pill">Enterprise Sync</span></div></div><p>Bi-directional enterprise timesheet synchronization endpoint for automated organization billing and time tracking.</p></div><div class="integration-card-foot"><span class="status-pill-badge connected">● Connected</span><button class="button primary sm">Configure</button></div></article><article class="integration-card"><div><div class="integration-card-top"><div class="integration-card-icon github">🐙</div><div><h3>GitHub Org</h3><span class="category-pill">Development</span></div></div><p>Auto-generate work log entries when PRs are reviewed, commits are pushed, or issues are closed in your repository.</p></div><div class="integration-card-foot"><span class="status-pill-badge active">Available</span><button class="button secondary-btn sm">Connect</button></div></article><article class="integration-card"><div><div class="integration-card-top"><div class="integration-card-icon slack">💬</div><div><h3>Slack Workspace</h3><span class="category-pill">Communication</span></div></div><p>Use <code>/log-work</code> slash commands or status updates in Slack to quickly record time entries without leaving chat.</p></div><div class="integration-card-foot"><span class="status-pill-badge active">Available</span><button class="button secondary-btn sm">Connect</button></div></article><article class="integration-card"><div><div class="integration-card-top"><div class="integration-card-icon jira">🎯</div><div><h3>Jira Software</h3><span class="category-pill">Project Management</span></div></div><p>Sync sprint tasks, issue status changes, and logged work hours automatically with Jira Cloud &amp; Data Center.</p></div><div class="integration-card-foot"><span class="status-pill-badge active">Available</span><button class="button secondary-btn sm">Connect</button></div></article><article class="integration-card"><div><div class="integration-card-top"><div class="integration-card-icon trello">📋</div><div><h3>Trello Boards</h3><span class="category-pill">Kanban Boards</span></div></div><p>Sync board cards, due dates, and checklist items with your My Tracker to-do tasks and timesheet logs.</p></div><div class="integration-card-foot"><span class="status-pill-badge active">Available</span><button class="button secondary-btn sm">Connect</button></div></article><article class="integration-card"><div><div class="integration-card-top"><div class="integration-card-icon drive">📁</div><div><h3>Google Drive &amp; Sheets</h3><span class="category-pill">Cloud Storage</span></div></div><p>Export work logs directly into Google Sheets spreadsheets or link Google Docs deliverables to tasks.</p></div><div class="integration-card-foot"><span class="status-pill-badge active">Available</span><button class="button secondary-btn sm">Connect</button></div></article><article class="integration-card"><div><div class="integration-card-top"><div class="integration-card-icon linear">⚡</div><div><h3>Linear App</h3><span class="category-pill">Issue Tracking</span></div></div><p>Sync Linear cycles, issues, and PR links with your time tracking workspace.</p></div><div class="integration-card-foot"><span class="status-pill-badge active">Available</span><button class="button secondary-btn sm">Connect</button></div></article><article class="integration-card"><div><div class="integration-card-top"><div class="integration-card-icon vscode">💻</div><div><h3>VS Code Extension</h3><span class="category-pill">IDE Tooling</span></div></div><p>Track active coding time per project file directly inside VS Code with status bar timer widget.</p></div><div class="integration-card-foot"><span class="status-pill-badge active">Available</span><button class="button secondary-btn sm">Install Widget</button></div></article></div></div>`);
				else if (unref(activeIntegrationTab) === "developer_api") {
					_push(`<div class="developer-api-section"><div class="panel-head" style="${ssrRenderStyle({
						"display": "flex",
						"align-items": "center",
						"justify-content": "space-between",
						"margin-bottom": "16px"
					})}"><div><h3>Developer API Keys &amp; Authentication</h3><p class="muted">Generate secret API tokens to authenticate REST API requests, webhooks, and external integrations.</p></div><button class="button primary"> ＋ Generate New API Key </button></div><section class="panel api-keys-panel" style="${ssrRenderStyle({ "margin-bottom": "24px" })}"><div class="panel table-panel"><table><thead><tr><th>Key Name</th><th>Secret API Key (Token)</th><th>Created Date</th><th>Expiration (Validity)</th><th>Scopes</th><th>Status</th><th style="${ssrRenderStyle({ "text-align": "right" })}">Actions</th></tr></thead><tbody><!--[-->`);
					ssrRenderList(unref(data).apiKeys || [], (k) => {
						_push(`<tr><td><b>${ssrInterpolate(k.name)}</b></td><td><div style="${ssrRenderStyle({
							"display": "flex",
							"align-items": "center",
							"gap": "8px"
						})}"><code class="api-key-code" style="${ssrRenderStyle({
							"font-family": "monospace",
							"font-size": "12px",
							"background": "var(--bg-subtle, #f1f5f9)",
							"padding": "4px 8px",
							"border-radius": "4px"
						})}">${ssrInterpolate(k.key.slice(0, 16))}•••••••• </code><button type="button" class="btn-action-icon" title="Copy Full Secret API Key">${ssrInterpolate(unref(copySuccessId) === k.id ? "✓ Copied" : "📋 Copy")}</button></div></td><td>${ssrInterpolate(k.createdDate)}</td><td><div class="validity-badge-year" title="Default validity set to 1 Year from creation date"> 📅 ${ssrInterpolate(k.expiryDate ? `Valid until ${k.expiryDate}` : "1 Year (Default)")}</div></td><td><div style="${ssrRenderStyle({
							"display": "flex",
							"gap": "4px",
							"flex-wrap": "wrap"
						})}"><!--[-->`);
						ssrRenderList(k.scopes || ["Read Logs", "Write Entries"], (s) => {
							_push(`<span class="tag-badge-item" style="${ssrRenderStyle({
								"font-size": "10px",
								"padding": "2px 6px"
							})}">${ssrInterpolate(s)}</span>`);
						});
						_push(`<!--]--></div></td><td><span class="${ssrRenderClass([k.status === "revoked" ? "revoked" : "active", "status-pill-badge"])}"> ● ${ssrInterpolate(k.status === "revoked" ? "Revoked" : "Active")}</span></td><td style="${ssrRenderStyle({ "text-align": "right" })}"><div style="${ssrRenderStyle({
							"display": "flex",
							"align-items": "center",
							"justify-content": "flex-end",
							"gap": "8px"
						})}">`);
						if (k.status !== "revoked") _push(`<button class="button danger-btn sm" title="Revoke this API Key immediately"> Revoke </button>`);
						else _push(`<!---->`);
						_push(`<button class="button text-link sm" style="${ssrRenderStyle({
							"color": "var(--muted)",
							"font-size": "11px"
						})}" title="Remove key from list"> Delete </button></div></td></tr>`);
					});
					_push(`<!--]-->`);
					if (!unref(data).apiKeys || !unref(data).apiKeys.length) _push(`<tr><td colspan="7" style="${ssrRenderStyle({
						"text-align": "center",
						"padding": "24px"
					})}" class="muted"> No API keys generated yet. Click &quot;+ Generate New API Key&quot; above to create one. </td></tr>`);
					else _push(`<!---->`);
					_push(`</tbody></table></div></section><section class="panel webhook-docs-card" style="${ssrRenderStyle({ "padding": "20px" })}"><div style="${ssrRenderStyle({
						"display": "flex",
						"align-items": "center",
						"justify-content": "space-between",
						"margin-bottom": "12px"
					})}"><div><h3 style="${ssrRenderStyle({ "margin": "0 0 4px" })}">📡 REST API &amp; Webhook Specifications</h3><p class="muted" style="${ssrRenderStyle({
						"margin": "0",
						"font-size": "12px"
					})}">Programmatically push timesheet logs or sync tasks into My Tracker via HTTP endpoints.</p></div><button class="button secondary-btn sm"> ⚡ Test Endpoint </button></div><div class="code-snippet-box" style="${ssrRenderStyle({
						"background": "#0f172a",
						"color": "#f8fafc",
						"padding": "16px",
						"border-radius": "8px",
						"font-family": "monospace",
						"font-size": "12px",
						"line-height": "1.6",
						"overflow-x": "auto"
					})}"><div style="${ssrRenderStyle({
						"color": "#94a3b8",
						"margin-bottom": "8px"
					})}"># Send a POST request with your 1-Year Valid API Key to log work session</div><div><span style="${ssrRenderStyle({ "color": "#38bdf8" })}">curl</span> -X POST http://localhost:3000/api/v1/timesheets/sync \\</div><div> -H <span style="${ssrRenderStyle({ "color": "#a5f3fc" })}">&quot;Authorization: Bearer ${ssrInterpolate(unref(data).apiKeys?.[0]?.key || "mytrk_live_sk_9a8f7e6d5c4b3a21")}&quot;</span> \\</div><div> -H <span style="${ssrRenderStyle({ "color": "#a5f3fc" })}">&quot;Content-Type: application/json&quot;</span> \\</div><div> -d <span style="${ssrRenderStyle({ "color": "#fde047" })}">&#39;{&quot;task&quot;: &quot;Figma Design &amp; MCP Sync&quot;, &quot;project&quot;: &quot;Website Redesign&quot;, &quot;hours&quot;: 2.5, &quot;user&quot;: &quot;TEZ&quot;}&#39;</span></div></div></section></div>`);
				} else _push(`<!---->`);
				_push(`</section>`);
			} else _push(`<section class="screen"><div class="page-head"><h1>${ssrInterpolate(unref(screen))}</h1></div><section class="panel empty">Workspace view ready.</section></section>`);
			_push(`</main></section>`);
			if (unref(showGenerateKeyModal)) _push(`<div class="backdrop"><form class="modal" style="${ssrRenderStyle({ "max-width": "480px" })}"><button type="button" class="close">×</button><p class="eyebrow">DEVELOPER CREDENTIALS</p><h2>Generate New API Key</h2><p class="muted" style="${ssrRenderStyle({
				"font-size": "12px",
				"margin-bottom": "16px"
			})}"> Create a new secret API token to connect your external scripts, Figma plugins, or MCP AI agents. </p><label class="form-label" style="${ssrRenderStyle({ "margin-top": "0" })}"> API Key Name <span class="req-star">*</span></label><input${ssrRenderAttr("value", unref(keyModalForm).name)} required placeholder="e.g. Production Figma Sync Key, MCP Agent Node" style="${ssrRenderStyle({ "margin-bottom": "14px" })}"><label class="form-label"> Expiration / Validity Period </label><select style="${ssrRenderStyle({ "margin-bottom": "14px" })}"><option value="1year"${ssrIncludeBooleanAttr(Array.isArray(unref(keyModalForm).expirationOption) ? ssrLooseContain(unref(keyModalForm).expirationOption, "1year") : ssrLooseEqual(unref(keyModalForm).expirationOption, "1year")) ? " selected" : ""}>1 Year (Valid until ${ssrInterpolate(new Date(Date.now() + 31536e6).toISOString().slice(0, 10))}) — Default</option><option value="90days"${ssrIncludeBooleanAttr(Array.isArray(unref(keyModalForm).expirationOption) ? ssrLooseContain(unref(keyModalForm).expirationOption, "90days") : ssrLooseEqual(unref(keyModalForm).expirationOption, "90days")) ? " selected" : ""}>90 Days</option><option value="30days"${ssrIncludeBooleanAttr(Array.isArray(unref(keyModalForm).expirationOption) ? ssrLooseContain(unref(keyModalForm).expirationOption, "30days") : ssrLooseEqual(unref(keyModalForm).expirationOption, "30days")) ? " selected" : ""}>30 Days</option><option value="never"${ssrIncludeBooleanAttr(Array.isArray(unref(keyModalForm).expirationOption) ? ssrLooseContain(unref(keyModalForm).expirationOption, "never") : ssrLooseEqual(unref(keyModalForm).expirationOption, "never")) ? " selected" : ""}>Never Expires</option></select><label class="form-label"> Permissions &amp; Scopes </label><div style="${ssrRenderStyle({
				"display": "flex",
				"flex-direction": "column",
				"gap": "8px",
				"margin-bottom": "20px",
				"background": "var(--bg-subtle, #f8fafc)",
				"padding": "12px",
				"border-radius": "8px",
				"border": "1px solid var(--line, #e2e8f0)"
			})}"><label style="${ssrRenderStyle({
				"display": "flex",
				"align-items": "center",
				"gap": "8px",
				"font-size": "12px",
				"cursor": "pointer"
			})}"><input type="checkbox" value="Read Logs"${ssrIncludeBooleanAttr(Array.isArray(unref(keyModalForm).scopes) ? ssrLooseContain(unref(keyModalForm).scopes, "Read Logs") : unref(keyModalForm).scopes) ? " checked" : ""}><span><b>Read Logs:</b> Fetch tasks and timesheet records</span></label><label style="${ssrRenderStyle({
				"display": "flex",
				"align-items": "center",
				"gap": "8px",
				"font-size": "12px",
				"cursor": "pointer"
			})}"><input type="checkbox" value="Write Entries"${ssrIncludeBooleanAttr(Array.isArray(unref(keyModalForm).scopes) ? ssrLooseContain(unref(keyModalForm).scopes, "Write Entries") : unref(keyModalForm).scopes) ? " checked" : ""}><span><b>Write Entries:</b> Push new work logs and time entries</span></label><label style="${ssrRenderStyle({
				"display": "flex",
				"align-items": "center",
				"gap": "8px",
				"font-size": "12px",
				"cursor": "pointer"
			})}"><input type="checkbox" value="Auto Sync"${ssrIncludeBooleanAttr(Array.isArray(unref(keyModalForm).scopes) ? ssrLooseContain(unref(keyModalForm).scopes, "Auto Sync") : unref(keyModalForm).scopes) ? " checked" : ""}><span><b>Auto Sync:</b> Trigger Office Timesheets &amp; Figma sync</span></label></div><div style="${ssrRenderStyle({
				"display": "flex",
				"align-items": "center",
				"justify-content": "flex-end",
				"gap": "10px"
			})}"><button type="button" class="button secondary-btn">Cancel</button><button type="submit" class="button primary">Generate Key</button></div></form></div>`);
			else _push(`<!---->`);
			if (unref(modal) === "api-key-created" && unref(createdKeySecretObj)) _push(`<div class="backdrop"><div class="modal" style="${ssrRenderStyle({ "max-width": "520px" })}"><button type="button" class="close">×</button><p class="eyebrow" style="${ssrRenderStyle({ "color": "#10b981" })}">✓ KEY GENERATED SUCCESSFULLY</p><h2>Your Secret API Key</h2><p class="muted" style="${ssrRenderStyle({
				"font-size": "12px",
				"margin-bottom": "12px"
			})}"> Please copy your secret key now. For security, full keys are valid until <b>1 Year</b> from creation date. </p><div class="secret-key-card"><label style="${ssrRenderStyle({
				"font-size": "11px",
				"color": "#94a3b8",
				"display": "block",
				"margin-bottom": "6px"
			})}">API KEY NAME: ${ssrInterpolate(unref(createdKeySecretObj).name)}</label><div class="secret-key-value"><code>${ssrInterpolate(unref(createdKeySecretObj).key)}</code><button type="button" class="button primary sm" style="${ssrRenderStyle({
				"font-size": "11px",
				"padding": "6px 12px"
			})}">${ssrInterpolate(unref(copySuccessId) === "secret-reveal" ? "✓ Copied!" : "📋 Copy Key")}</button></div><div style="${ssrRenderStyle({
				"margin-top": "10px",
				"font-size": "11px",
				"color": "#a7f3d0",
				"display": "flex",
				"align-items": "center",
				"gap": "6px"
			})}"><span>📅</span> Valid until: <b>${ssrInterpolate(unref(createdKeySecretObj).expiryDate || "1 Year")}</b></div></div><div style="${ssrRenderStyle({
				"display": "flex",
				"justify-content": "flex-end"
			})}"><button type="button" class="button primary">Done</button></div></div></div>`);
			else _push(`<!---->`);
			if (unref(keyToRevoke)) _push(`<div class="backdrop"><div class="modal" style="${ssrRenderStyle({ "max-width": "420px" })}"><button type="button" class="close">×</button><p class="eyebrow" style="${ssrRenderStyle({ "color": "#ef4444" })}">REVOKE API KEY</p><h2>Revoke API Key?</h2><p class="muted" style="${ssrRenderStyle({
				"font-size": "13px",
				"margin-bottom": "20px"
			})}"> Are you sure you want to revoke key <b>&quot;${ssrInterpolate(unref(keyToRevoke).name)}&quot;</b>? External scripts or plugins using this key will immediately lose access. </p><div style="${ssrRenderStyle({
				"display": "flex",
				"align-items": "center",
				"justify-content": "flex-end",
				"gap": "10px"
			})}"><button type="button" class="button secondary-btn">Cancel</button><button type="button" class="button danger-btn"> Confirm Revoke </button></div></div></div>`);
			else _push(`<!---->`);
			if (unref(activeConnectorModal)) {
				_push(`<div class="backdrop"><div class="modal" style="${ssrRenderStyle({ "max-width": "480px" })}"><button type="button" class="close">×</button>`);
				if (unref(activeConnectorModal) === "figma") _push(`<form><p class="eyebrow">FIGMA WORKSPACE CONNECTOR</p><h2>Configure Figma Integration</h2><p class="muted" style="${ssrRenderStyle({
					"font-size": "12px",
					"margin-bottom": "14px"
				})}">Sync design component hours and canvas frame time into My Tracker.</p><label class="form-label" style="${ssrRenderStyle({ "margin-top": "0" })}">Figma Team ID / Organization Handle</label><input${ssrRenderAttr("value", unref(figmaForm).teamId)} required style="${ssrRenderStyle({ "margin-bottom": "14px" })}"><label class="custom-toggle-label" style="${ssrRenderStyle({
					"display": "flex",
					"align-items": "center",
					"gap": "8px",
					"font-size": "13px",
					"margin-bottom": "20px"
				})}"><input type="checkbox"${ssrIncludeBooleanAttr(Array.isArray(unref(figmaForm).autoLogFrames) ? ssrLooseContain(unref(figmaForm).autoLogFrames, null) : unref(figmaForm).autoLogFrames) ? " checked" : ""}><span>Auto-create work log entries on Figma frame edits</span></label><div style="${ssrRenderStyle({
					"display": "flex",
					"justify-content": "flex-end",
					"gap": "10px"
				})}"><button type="button" class="button secondary-btn">Cancel</button><button type="submit" class="button primary">Save Connection</button></div></form>`);
				else if (unref(activeConnectorModal) === "mcp") _push(`<form><p class="eyebrow">MODEL CONTEXT PROTOCOL (MCP)</p><h2>Configure MCP AI Agent Server</h2><p class="muted" style="${ssrRenderStyle({
					"font-size": "12px",
					"margin-bottom": "14px"
				})}">Connect local/remote MCP server nodes (Cursor, Antigravity, Claude) to log agent sessions.</p><label class="form-label" style="${ssrRenderStyle({ "margin-top": "0" })}">MCP Server URL / Port Endpoint</label><input${ssrRenderAttr("value", unref(mcpForm).serverUrl)} required style="${ssrRenderStyle({ "margin-bottom": "14px" })}"><label class="custom-toggle-label" style="${ssrRenderStyle({
					"display": "flex",
					"align-items": "center",
					"gap": "8px",
					"font-size": "13px",
					"margin-bottom": "20px"
				})}"><input type="checkbox"${ssrIncludeBooleanAttr(Array.isArray(unref(mcpForm).streamLogs) ? ssrLooseContain(unref(mcpForm).streamLogs, null) : unref(mcpForm).streamLogs) ? " checked" : ""}><span>Stream coding prompt metrics &amp; task logs automatically</span></label><div style="${ssrRenderStyle({
					"display": "flex",
					"justify-content": "flex-end",
					"gap": "10px"
				})}"><button type="button" class="button secondary-btn">Cancel</button><button type="submit" class="button primary">Connect MCP Node</button></div></form>`);
				else if (unref(activeConnectorModal) === "notion") _push(`<form><p class="eyebrow">NOTION WORKSPACE SYNC</p><h2>Configure Notion Integration</h2><p class="muted" style="${ssrRenderStyle({
					"font-size": "12px",
					"margin-bottom": "14px"
				})}">Link Notion databases and meeting note pages with your work log.</p><label class="form-label" style="${ssrRenderStyle({ "margin-top": "0" })}">Notion Database ID</label><input${ssrRenderAttr("value", unref(notionForm).databaseId)} required style="${ssrRenderStyle({ "margin-bottom": "14px" })}"><label class="custom-toggle-label" style="${ssrRenderStyle({
					"display": "flex",
					"align-items": "center",
					"gap": "8px",
					"font-size": "13px",
					"margin-bottom": "20px"
				})}"><input type="checkbox"${ssrIncludeBooleanAttr(Array.isArray(unref(notionForm).syncPages) ? ssrLooseContain(unref(notionForm).syncPages, null) : unref(notionForm).syncPages) ? " checked" : ""}><span>Import meeting notes into My Tracker automatically</span></label><div style="${ssrRenderStyle({
					"display": "flex",
					"justify-content": "flex-end",
					"gap": "10px"
				})}"><button type="button" class="button secondary-btn">Cancel</button><button type="submit" class="button primary">Link Database</button></div></form>`);
				else _push(`<div><h2>Configure ${ssrInterpolate(unref(activeConnectorModal))}</h2><p class="muted" style="${ssrRenderStyle({ "margin-bottom": "16px" })}">Connection endpoint active.</p><button class="button primary">Done</button></div>`);
				_push(`</div></div>`);
			} else _push(`<!---->`);
			if (unref(modal) === "task" || unref(modal) === "log") {
				_push(`<div class="backdrop"><form class="modal"><button type="button" class="close">×</button><p class="eyebrow">WORK LOG ENTRY</p><h2>Log time entry</h2><label class="form-label" style="${ssrRenderStyle({ "margin-top": "0" })}"> Task Name <span class="req-star">*</span></label><input${ssrRenderAttr("value", unref(task).task)} required placeholder="What did you work on? (e.g. Navigation UI Refactor)"><div class="custom-dropdown-container"><label class="form-label"> Project Name <span class="req-star">*</span></label><div class="custom-combobox"><input${ssrRenderAttr("value", unref(task).project)} placeholder="Select existing or type new project name..." required><span class="dropdown-chevron">▾</span>`);
				if (unref(activeDropdown) === "project") {
					_push(`<div class="custom-dropdown-menu"><div class="dropdown-menu-title">Select Project (Single Select)</div><!--[-->`);
					ssrRenderList(unref(filteredProjects), (p) => {
						_push(`<div class="${ssrRenderClass([{ selected: unref(task).project === p }, "dropdown-option-item"])}"><span>📁 ${ssrInterpolate(p)}</span><div style="${ssrRenderStyle({
							"display": "flex",
							"align-items": "center",
							"gap": "6px"
						})}">`);
						if (unref(task).project === p) _push(`<span class="check-icon">✓</span>`);
						else _push(`<!---->`);
						_push(`<button type="button" class="btn-del-option" title="Delete project"> ✕ </button></div></div>`);
					});
					_push(`<!--]-->`);
					if (unref(task).project.trim() && !unref(data).projects.includes(unref(task).project.trim())) _push(`<div class="dropdown-option-item create-new"> ➕ Add &quot;<b>${ssrInterpolate(unref(task).project.trim())}</b>&quot; as new project </div>`);
					else _push(`<!---->`);
					_push(`</div>`);
				} else _push(`<!---->`);
				_push(`</div></div><div class="custom-dropdown-container"><label class="form-label"> Sync with team member(s) </label><div class="custom-combobox"><input${ssrRenderAttr("value", unref(personInputText))} placeholder="Select or type team member name (press Enter)..."><span class="dropdown-chevron">▾</span>`);
				if (unref(activeDropdown) === "person") {
					_push(`<div class="custom-dropdown-menu"><div class="dropdown-menu-title">Select Team Members (Multi-select)</div><!--[-->`);
					ssrRenderList(unref(filteredUsers), (u) => {
						_push(`<div class="${ssrRenderClass([{ selected: unref(task).owners.includes(u) }, "dropdown-option-item"])}"><div style="${ssrRenderStyle({
							"display": "flex",
							"align-items": "center",
							"gap": "8px"
						})}"><span class="avatar-badge mini" style="${ssrRenderStyle({
							backgroundColor: getAvatarColor(u),
							color: "#fff"
						})}">${ssrInterpolate(u.charAt(0).toUpperCase())}</span><span>${ssrInterpolate(u)}</span></div><div style="${ssrRenderStyle({
							"display": "flex",
							"align-items": "center",
							"gap": "6px"
						})}">`);
						if (unref(task).owners.includes(u)) _push(`<span class="check-icon">✓</span>`);
						else _push(`<!---->`);
						_push(`<button type="button" class="btn-del-option" title="Delete member"> ✕ </button></div></div>`);
					});
					_push(`<!--]-->`);
					if (unref(personInputText).trim() && !unref(data).users.includes(unref(personInputText).trim())) _push(`<div class="dropdown-option-item create-new"> ➕ Add &quot;<b>${ssrInterpolate(unref(personInputText).trim())}</b>&quot; as new member </div>`);
					else _push(`<!---->`);
					_push(`</div>`);
				} else _push(`<!---->`);
				_push(`</div>`);
				if (unref(task).owners.length) {
					_push(`<div class="tag-badges-wrapper"><!--[-->`);
					ssrRenderList(unref(task).owners, (p) => {
						_push(`<span class="tag-badge-item"><span class="avatar-badge micro" style="${ssrRenderStyle({
							backgroundColor: "rgba(255,255,255,0.25)",
							color: "#fff"
						})}">${ssrInterpolate(p.charAt(0).toUpperCase())}</span> ${ssrInterpolate(p)} <button type="button">✕</button></span>`);
					});
					_push(`<!--]--></div>`);
				} else _push(`<!---->`);
				_push(`</div><div class="custom-dropdown-container"><label class="form-label"> Add tag </label><div class="custom-combobox"><input${ssrRenderAttr("value", unref(tagInputText))} placeholder="Select or type tag name (press Enter)..."><span class="dropdown-chevron">▾</span>`);
				if (unref(activeDropdown) === "tag") {
					_push(`<div class="custom-dropdown-menu"><div class="dropdown-menu-title">Select Tags (Multi-select)</div><!--[-->`);
					ssrRenderList(unref(filteredTags), (t) => {
						_push(`<div class="${ssrRenderClass([{ selected: unref(task).tags.includes(t) }, "dropdown-option-item"])}"><span>🏷 ${ssrInterpolate(t)}</span><div style="${ssrRenderStyle({
							"display": "flex",
							"align-items": "center",
							"gap": "6px"
						})}">`);
						if (unref(task).tags.includes(t)) _push(`<span class="check-icon">✓</span>`);
						else _push(`<!---->`);
						_push(`<button type="button" class="btn-del-option" title="Delete tag"> ✕ </button></div></div>`);
					});
					_push(`<!--]-->`);
					if (unref(tagInputText).trim() && !unref(availableTags).includes(unref(tagInputText).trim())) _push(`<div class="dropdown-option-item create-new"> ➕ Add &quot;<b>${ssrInterpolate(unref(tagInputText).trim())}</b>&quot; as new tag </div>`);
					else _push(`<!---->`);
					_push(`</div>`);
				} else _push(`<!---->`);
				_push(`</div>`);
				if (unref(task).tags.length) {
					_push(`<div class="tag-badges-wrapper"><!--[-->`);
					ssrRenderList(unref(task).tags, (tag) => {
						_push(`<span class="tag-badge-item">${ssrInterpolate(tag)} <button type="button">✕</button></span>`);
					});
					_push(`<!--]--></div>`);
				} else _push(`<!---->`);
				_push(`</div><div class="schedule-card-box"><div class="schedule-card-title"> 📅 Session Schedule &amp; Timing </div><div class="form-group-row"><div><label class="form-label" style="${ssrRenderStyle({ "margin-top": "0" })}">Start Date</label><input${ssrRenderAttr("value", unref(task).startDate)} type="date" required></div><div><label class="form-label" style="${ssrRenderStyle({ "margin-top": "0" })}">End Date</label><input${ssrRenderAttr("value", unref(task).endDate)}${ssrRenderAttr("min", unref(task).startDate)} type="date" required></div></div><div class="form-group-row" style="${ssrRenderStyle({ "margin-bottom": "0" })}"><div><label class="form-label" style="${ssrRenderStyle({ "margin-top": "0" })}">Start Time</label><input${ssrRenderAttr("value", unref(task).startTime)} type="time" required></div><div><label class="form-label" style="${ssrRenderStyle({ "margin-top": "0" })}">End Time</label><input${ssrRenderAttr("value", unref(task).endTime)} type="time" required></div></div></div><button type="submit" class="button primary full" style="${ssrRenderStyle({ "margin-top": "16px" })}">Save work log</button></form></div>`);
			} else _push(`<!---->`);
			if (unref(modal) === "edit-log") {
				_push(`<div class="backdrop"><form class="modal"><button type="button" class="close">×</button><p class="eyebrow">EDIT LOG ENTRY</p><h2>Update work log</h2><label class="form-label" style="${ssrRenderStyle({ "margin-top": "0" })}"> Task Name <span class="req-star">*</span></label><input${ssrRenderAttr("value", unref(editLogData).task)} required placeholder="What did you work on? (e.g. Navigation UI Refactor)"><div class="custom-dropdown-container"><label class="form-label"> Project Name <span class="req-star">*</span></label><div class="custom-combobox"><input${ssrRenderAttr("value", unref(editLogData).project)} placeholder="Select existing or type new project name..." required><span class="dropdown-chevron">▾</span>`);
				if (unref(activeEditDropdown) === "project") {
					_push(`<div class="custom-dropdown-menu"><div class="dropdown-menu-title">Select Project (Single Select)</div><!--[-->`);
					ssrRenderList(unref(filteredEditProjects), (p) => {
						_push(`<div class="${ssrRenderClass([{ selected: unref(editLogData).project === p }, "dropdown-option-item"])}"><span>📁 ${ssrInterpolate(p)}</span><div style="${ssrRenderStyle({
							"display": "flex",
							"align-items": "center",
							"gap": "6px"
						})}">`);
						if (unref(editLogData).project === p) _push(`<span class="check-icon">✓</span>`);
						else _push(`<!---->`);
						_push(`<button type="button" class="btn-del-option" title="Delete project"> ✕ </button></div></div>`);
					});
					_push(`<!--]-->`);
					if (unref(editLogData).project.trim() && !unref(data).projects.includes(unref(editLogData).project.trim())) _push(`<div class="dropdown-option-item create-new"> ➕ Add &quot;<b>${ssrInterpolate(unref(editLogData).project.trim())}</b>&quot; as new project </div>`);
					else _push(`<!---->`);
					_push(`</div>`);
				} else _push(`<!---->`);
				_push(`</div></div><div class="custom-dropdown-container"><label class="form-label"> Sync with team member(s) </label><div class="custom-combobox"><input${ssrRenderAttr("value", unref(editPersonInputText))} placeholder="Select or type team member name (press Enter)..."><span class="dropdown-chevron">▾</span>`);
				if (unref(activeEditDropdown) === "person") {
					_push(`<div class="custom-dropdown-menu"><div class="dropdown-menu-title">Select Team Members (Multi-select)</div><!--[-->`);
					ssrRenderList(unref(filteredEditUsers), (u) => {
						_push(`<div class="${ssrRenderClass([{ selected: unref(editLogData).owners.includes(u) }, "dropdown-option-item"])}"><div style="${ssrRenderStyle({
							"display": "flex",
							"align-items": "center",
							"gap": "8px"
						})}"><span class="avatar-badge mini" style="${ssrRenderStyle({
							backgroundColor: getAvatarColor(u),
							color: "#fff"
						})}">${ssrInterpolate(u.charAt(0).toUpperCase())}</span><span>${ssrInterpolate(u)}</span></div><div style="${ssrRenderStyle({
							"display": "flex",
							"align-items": "center",
							"gap": "6px"
						})}">`);
						if (unref(editLogData).owners.includes(u)) _push(`<span class="check-icon">✓</span>`);
						else _push(`<!---->`);
						_push(`<button type="button" class="btn-del-option" title="Delete member"> ✕ </button></div></div>`);
					});
					_push(`<!--]-->`);
					if (unref(editPersonInputText).trim() && !unref(data).users.includes(unref(editPersonInputText).trim())) _push(`<div class="dropdown-option-item create-new"> ➕ Add &quot;<b>${ssrInterpolate(unref(editPersonInputText).trim())}</b>&quot; as new member </div>`);
					else _push(`<!---->`);
					_push(`</div>`);
				} else _push(`<!---->`);
				_push(`</div>`);
				if (unref(editLogData).owners.length) {
					_push(`<div class="tag-badges-wrapper"><!--[-->`);
					ssrRenderList(unref(editLogData).owners, (p) => {
						_push(`<span class="tag-badge-item"><span class="avatar-badge micro" style="${ssrRenderStyle({
							backgroundColor: "rgba(255,255,255,0.25)",
							color: "#fff"
						})}">${ssrInterpolate(p.charAt(0).toUpperCase())}</span> ${ssrInterpolate(p)} <button type="button">✕</button></span>`);
					});
					_push(`<!--]--></div>`);
				} else _push(`<!---->`);
				_push(`</div><div class="custom-dropdown-container"><label class="form-label"> Add tag </label><div class="custom-combobox"><input${ssrRenderAttr("value", unref(editTagInputText))} placeholder="Select or type tag name (press Enter)..."><span class="dropdown-chevron">▾</span>`);
				if (unref(activeEditDropdown) === "tag") {
					_push(`<div class="custom-dropdown-menu"><div class="dropdown-menu-title">Select Tags (Multi-select)</div><!--[-->`);
					ssrRenderList(unref(filteredEditTags), (t) => {
						_push(`<div class="${ssrRenderClass([{ selected: unref(editLogData).tags.includes(t) }, "dropdown-option-item"])}"><span>🏷 ${ssrInterpolate(t)}</span><div style="${ssrRenderStyle({
							"display": "flex",
							"align-items": "center",
							"gap": "6px"
						})}">`);
						if (unref(editLogData).tags.includes(t)) _push(`<span class="check-icon">✓</span>`);
						else _push(`<!---->`);
						_push(`<button type="button" class="btn-del-option" title="Delete tag"> ✕ </button></div></div>`);
					});
					_push(`<!--]-->`);
					if (unref(editTagInputText).trim() && !unref(availableTags).includes(unref(editTagInputText).trim())) _push(`<div class="dropdown-option-item create-new"> ➕ Add &quot;<b>${ssrInterpolate(unref(editTagInputText).trim())}</b>&quot; as new tag </div>`);
					else _push(`<!---->`);
					_push(`</div>`);
				} else _push(`<!---->`);
				_push(`</div>`);
				if (unref(editLogData).tags.length) {
					_push(`<div class="tag-badges-wrapper"><!--[-->`);
					ssrRenderList(unref(editLogData).tags, (tag) => {
						_push(`<span class="tag-badge-item">${ssrInterpolate(tag)} <button type="button">✕</button></span>`);
					});
					_push(`<!--]--></div>`);
				} else _push(`<!---->`);
				_push(`</div><div class="schedule-card-box"><div class="schedule-card-title"> 📅 Session Schedule &amp; Timing </div><div class="form-group-row"><div><label class="form-label" style="${ssrRenderStyle({ "margin-top": "0" })}">Start Date</label><input${ssrRenderAttr("value", unref(editLogData).startDate)} type="date" required></div><div><label class="form-label" style="${ssrRenderStyle({ "margin-top": "0" })}">End Date</label><input${ssrRenderAttr("value", unref(editLogData).endDate)}${ssrRenderAttr("min", unref(editLogData).startDate)} type="date" required></div></div><div class="form-group-row" style="${ssrRenderStyle({ "margin-bottom": "0" })}"><div><label class="form-label" style="${ssrRenderStyle({ "margin-top": "0" })}">Start Time</label><input${ssrRenderAttr("value", unref(editLogData).startTime)} type="time" required></div><div><label class="form-label" style="${ssrRenderStyle({ "margin-top": "0" })}">End Time</label><input${ssrRenderAttr("value", unref(editLogData).endTime)} type="time" required></div></div></div><button type="submit" class="button primary full" style="${ssrRenderStyle({ "margin-top": "16px" })}">Save changes</button></form></div>`);
			} else _push(`<!---->`);
			if (unref(modal) === "edit-todo" && unref(editingTodo)) {
				_push(`<div class="backdrop"><form class="modal"><button type="button" class="close">×</button><p class="eyebrow">EDIT ACTION ITEM</p><h2>Update action item</h2><label class="form-label" style="${ssrRenderStyle({ "margin-top": "0" })}"> Task Name <span class="req-star">*</span></label><input${ssrRenderAttr("value", unref(editingTodo).title)} required placeholder="What needs doing? (e.g. Design Dashboard Components)"><label class="form-label">Description / Context</label><textarea rows="2" placeholder="Add any details, notes, or instructions...">${ssrInterpolate(unref(editingTodo).detail)}</textarea><div class="form-group-row"><div><label class="form-label">Priority</label><select><option value="High"${ssrIncludeBooleanAttr(Array.isArray(unref(editingTodo).priority) ? ssrLooseContain(unref(editingTodo).priority, "High") : ssrLooseEqual(unref(editingTodo).priority, "High")) ? " selected" : ""}>High Priority</option><option value="Medium"${ssrIncludeBooleanAttr(Array.isArray(unref(editingTodo).priority) ? ssrLooseContain(unref(editingTodo).priority, "Medium") : ssrLooseEqual(unref(editingTodo).priority, "Medium")) ? " selected" : ""}>Medium Priority</option><option value="Low"${ssrIncludeBooleanAttr(Array.isArray(unref(editingTodo).priority) ? ssrLooseContain(unref(editingTodo).priority, "Low") : ssrLooseEqual(unref(editingTodo).priority, "Low")) ? " selected" : ""}>Low Priority</option></select></div><div><label class="form-label">Due Date</label><input${ssrRenderAttr("value", unref(editingTodo).due)} type="date"></div></div><label class="form-label">Status Stage</label><select><option value="Pending"${ssrIncludeBooleanAttr(Array.isArray(unref(editingTodo).status) ? ssrLooseContain(unref(editingTodo).status, "Pending") : ssrLooseEqual(unref(editingTodo).status, "Pending")) ? " selected" : ""}>Pending</option><option value="In Progress"${ssrIncludeBooleanAttr(Array.isArray(unref(editingTodo).status) ? ssrLooseContain(unref(editingTodo).status, "In Progress") : ssrLooseEqual(unref(editingTodo).status, "In Progress")) ? " selected" : ""}>In Progress</option><option value="Completed"${ssrIncludeBooleanAttr(Array.isArray(unref(editingTodo).status) ? ssrLooseContain(unref(editingTodo).status, "Completed") : ssrLooseEqual(unref(editingTodo).status, "Completed")) ? " selected" : ""}>Completed</option></select><label class="form-label">Reference Link</label><div class="input-with-icon"><span class="input-icon">🔗</span><input${ssrRenderAttr("value", unref(editingTodo).link)} placeholder="e.g. https://github.com/... or Figma link"></div><label class="form-label">Upload File / Attachment</label><div class="file-upload-box"><input type="file" hidden>`);
				if (!unref(editingTodo).fileName) _push(`<div class="file-upload-placeholder"><span>📁 Choose file to attach</span><small>Documents, images, designs, or specs</small></div>`);
				else _push(`<div class="file-uploaded-item"><span class="file-name">📎 ${ssrInterpolate(unref(editingTodo).fileName)}</span><button type="button" class="btn-remove-file" title="Remove attachment">✕</button></div>`);
				_push(`</div><button type="submit" class="button primary full" style="${ssrRenderStyle({ "margin-top": "16px" })}">Save changes</button></form></div>`);
			} else _push(`<!---->`);
			if (unref(modal) === "todo") {
				_push(`<div class="backdrop"><form class="modal"><button type="button" class="close">×</button><p class="eyebrow">NEW ACTION ITEM</p><h2>Add to-do item</h2><label class="form-label" style="${ssrRenderStyle({ "margin-top": "0" })}"> Task Name <span class="req-star">*</span></label><input${ssrRenderAttr("value", unref(todo).title)} required placeholder="What needs doing? (e.g. Design Dashboard Components)"><label class="form-label">Description / Context</label><textarea rows="2" placeholder="Add any details, notes, or instructions...">${ssrInterpolate(unref(todo).detail)}</textarea><div class="form-group-row"><div><label class="form-label">Priority</label><select><option value="High"${ssrIncludeBooleanAttr(Array.isArray(unref(todo).priority) ? ssrLooseContain(unref(todo).priority, "High") : ssrLooseEqual(unref(todo).priority, "High")) ? " selected" : ""}>High Priority</option><option value="Medium"${ssrIncludeBooleanAttr(Array.isArray(unref(todo).priority) ? ssrLooseContain(unref(todo).priority, "Medium") : ssrLooseEqual(unref(todo).priority, "Medium")) ? " selected" : ""}>Medium Priority</option><option value="Low"${ssrIncludeBooleanAttr(Array.isArray(unref(todo).priority) ? ssrLooseContain(unref(todo).priority, "Low") : ssrLooseEqual(unref(todo).priority, "Low")) ? " selected" : ""}>Low Priority</option></select></div><div><label class="form-label">Due Date</label><input${ssrRenderAttr("value", unref(todo).due)} type="date"></div></div><label class="form-label">Reference Link</label><div class="input-with-icon"><span class="input-icon">🔗</span><input${ssrRenderAttr("value", unref(todo).link)} placeholder="e.g. https://github.com/... or Figma link"></div><label class="form-label">Upload File / Attachment</label><div class="file-upload-box"><input type="file" hidden>`);
				if (!unref(todo).fileName) _push(`<div class="file-upload-placeholder"><span>📁 Choose file to attach</span><small>Documents, images, designs, or specs</small></div>`);
				else _push(`<div class="file-uploaded-item"><span class="file-name">📎 ${ssrInterpolate(unref(todo).fileName)}</span><button type="button" class="btn-remove-file" title="Remove attachment">✕</button></div>`);
				_push(`</div><button type="submit" class="button primary full" style="${ssrRenderStyle({ "margin-top": "16px" })}">Add to-do</button></form></div>`);
			} else _push(`<!---->`);
			if (unref(modal) === "export") {
				_push(`<div class="backdrop"><div class="modal export-modal"><button type="button" class="close">×</button><p class="eyebrow">${ssrInterpolate(unref(exportContext) === "timesheets" ? "TIMESHEET EXPORT" : "WORKSPACE REPORT")}</p><h2>${ssrInterpolate(unref(exportContext) === "timesheets" ? "Export Timesheets" : "Export Reports & Workspace Data")}</h2><p class="muted">${ssrInterpolate(unref(exportContext) === "timesheets" ? "Download a date-wise CSV of all timesheet logs across your selected date range." : "Choose your dataset and date range to export comprehensive workspace reports.")}</p>`);
				if (unref(exportContext) === "reports") _push(`<div class="export-section"><label class="export-label">SELECT DATA TYPE TO EXPORT</label><div class="export-pills-row"><button type="button" class="${ssrRenderClass([{ active: unref(exportDataType) === "timesheets" }, "export-pill"])}"> ⏱ Timesheet Logs </button><button type="button" class="${ssrRenderClass([{ active: unref(exportDataType) === "todos" }, "export-pill"])}"> ✓ To-Do Actions </button><button type="button" class="${ssrRenderClass([{ active: unref(exportDataType) === "all" }, "export-pill"])}"> 📊 All Records </button></div></div>`);
				else _push(`<!---->`);
				_push(`<div class="export-section"><label class="export-label">SELECT DATE RANGE</label><div class="export-pills-grid"><button type="button" class="${ssrRenderClass([{ active: unref(exportDateRange) === "current_month" }, "export-pill"])}"> 📅 Current Month </button><button type="button" class="${ssrRenderClass([{ active: unref(exportDateRange) === "last_3_months" }, "export-pill"])}"> 🗓 Last 3 Months </button><button type="button" class="${ssrRenderClass([{ active: unref(exportDateRange) === "last_6_months" }, "export-pill"])}"> 📆 Last 6 Months </button><button type="button" class="${ssrRenderClass([{ active: unref(exportDateRange) === "custom" }, "export-pill"])}"> ⚡ Custom Range </button></div></div>`);
				if (unref(exportDateRange) === "custom") _push(`<div class="custom-range-inputs"><div class="range-col"><label class="sub-label">FROM (START DATE)</label><input${ssrRenderAttr("value", unref(exportCustomStartDate))} type="date" class="range-input"></div><div class="range-col"><label class="sub-label">TO (END DATE)</label><input${ssrRenderAttr("value", unref(exportCustomEndDate))} type="date" class="range-input"></div></div>`);
				else _push(`<!---->`);
				_push(`<div class="export-section"><label class="export-label">EXPORT FILE FORMAT</label><div class="export-dropdown-wrap"><span class="export-format-icon">${ssrInterpolate(unref(exportFileFormat) === "pdf" ? "📑" : unref(exportFileFormat) === "docx" ? "📝" : "📄")}</span><select class="export-dropdown-select"><option value="csv"${ssrIncludeBooleanAttr(Array.isArray(unref(exportFileFormat)) ? ssrLooseContain(unref(exportFileFormat), "csv") : ssrLooseEqual(unref(exportFileFormat), "csv")) ? " selected" : ""}>CSV Spreadsheet (.csv)</option><option value="pdf"${ssrIncludeBooleanAttr(Array.isArray(unref(exportFileFormat)) ? ssrLooseContain(unref(exportFileFormat), "pdf") : ssrLooseEqual(unref(exportFileFormat), "pdf")) ? " selected" : ""}>PDF Document (.pdf)</option><option value="docx"${ssrIncludeBooleanAttr(Array.isArray(unref(exportFileFormat)) ? ssrLooseContain(unref(exportFileFormat), "docx") : ssrLooseEqual(unref(exportFileFormat), "docx")) ? " selected" : ""}>Word Document (.docx)</option></select><span class="export-dropdown-arrow">▾</span></div></div><div class="export-summary-card"><div class="summary-meta"><span class="summary-icon">📁</span><div><div class="summary-range-text">Date Range: <b>${ssrInterpolate(unref(computedExportRange).displayLabel)}</b></div><div class="summary-count-text">`);
				if (unref(exportContext) === "timesheets" || unref(exportDataType) === "timesheets") _push(`<span> Ready to export <b>${ssrInterpolate(unref(filteredExportTimesheets).length)}</b> date-wise timesheet log(s) </span>`);
				else if (unref(exportDataType) === "todos") _push(`<span> Ready to export <b>${ssrInterpolate(unref(filteredExportTodos).length)}</b> to-do action(s) </span>`);
				else _push(`<span> Ready to export <b>${ssrInterpolate(unref(filteredExportTimesheets).length)}</b> logs &amp; <b>${ssrInterpolate(unref(filteredExportTodos).length)}</b> to-dos </span>`);
				_push(`</div></div></div></div><div class="export-modal-actions"><button type="button" class="button">Cancel</button><button type="button" class="button primary"${ssrIncludeBooleanAttr(unref(exportContext) === "timesheets" || unref(exportDataType) === "timesheets" ? unref(filteredExportTimesheets).length === 0 : unref(exportDataType) === "todos" ? unref(filteredExportTodos).length === 0 : unref(filteredExportTimesheets).length === 0 && unref(filteredExportTodos).length === 0) ? " disabled" : ""}> ⇩ Download ${ssrInterpolate(unref(exportFileFormat).toUpperCase())}</button></div></div></div>`);
			} else _push(`<!---->`);
			if (unref(modal) === "api-key-created" && unref(newlyCreatedKeyObj)) _push(`<div class="backdrop"><div class="modal" style="${ssrRenderStyle({ "max-width": "520px" })}"><button class="close">×</button><p class="eyebrow">DEVELOPER ACCESS KEY</p><h2>API Key Generated</h2><p class="muted" style="${ssrRenderStyle({ "margin-bottom": "16px" })}"> Your API key for <b>${ssrInterpolate(unref(newlyCreatedKeyObj).name)}</b> has been created. Copy it now and save it securely. </p><div style="${ssrRenderStyle({
				"background": "#0f172a",
				"color": "#f8fafc",
				"padding": "14px 18px",
				"border-radius": "8px",
				"display": "flex",
				"align-items": "center",
				"justify-content": "space-between",
				"gap": "12px",
				"margin-bottom": "20px"
			})}"><code style="${ssrRenderStyle({
				"font-family": "monospace",
				"font-size": "13px",
				"word-break": "break-all",
				"color": "#38bdf8"
			})}">${ssrInterpolate(unref(newlyCreatedKeyObj).key)}</code><button class="button primary sm" style="${ssrRenderStyle({ "white-space": "nowrap" })}">${ssrInterpolate(unref(copySuccessId) === "modal-key" ? "✓ Copied!" : "📋 Copy Key")}</button></div><div style="${ssrRenderStyle({
				"display": "flex",
				"justify-content": "flex-end"
			})}"><button class="button primary">Done &amp; Close</button></div></div></div>`);
			else _push(`<!---->`);
			if (unref(importOpen)) _push(`<div class="backdrop"><section class="modal"><button class="close">×</button><p class="eyebrow">INTELLIGENT FILE IMPORT</p><h2>Review and import records</h2><pre style="${ssrRenderStyle({
				"background": "#f4f7fb",
				"padding": "12px",
				"border-radius": "8px",
				"font-size": "11px",
				"max-height": "180px",
				"overflow": "auto"
			})}">${ssrInterpolate(unref(importPreview))}</pre><div class="modal-actions" style="${ssrRenderStyle({
				"display": "flex",
				"gap": "10px",
				"margin-top": "15px"
			})}"><button class="button" style="${ssrRenderStyle({ "flex": "1" })}">Cancel</button><button class="button primary" style="${ssrRenderStyle({ "flex": "1" })}">Import into DB</button></div></section></div>`);
			else _push(`<!---->`);
			if (unref(toast)) _push(`<div class="toast">${ssrInterpolate(unref(toast))}</div>`);
			else _push(`<!---->`);
			_push(`<datalist id="project-list"><!--[-->`);
			ssrRenderList(unref(data).projects, (p) => {
				_push(`<option${ssrRenderAttr("value", p)}></option>`);
			});
			_push(`<!--]--></datalist><datalist id="user-list"><!--[-->`);
			ssrRenderList(unref(data).users, (u) => {
				_push(`<option${ssrRenderAttr("value", u)}></option>`);
			});
			_push(`<!--]--></datalist><datalist id="tag-list"><!--[-->`);
			ssrRenderList(predefinedTags, (t) => {
				_push(`<option${ssrRenderAttr("value", t)}></option>`);
			});
			_push(`<!--]--></datalist></div>`);
		};
	}
});
//#endregion
//#region app.vue
var _sfc_setup$2 = app_vue_vue_type_script_setup_true_lang_default.setup;
app_vue_vue_type_script_setup_true_lang_default.setup = (props, ctx) => {
	const ssrContext = useSSRContext();
	(ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("app.vue");
	return _sfc_setup$2 ? _sfc_setup$2(props, ctx) : void 0;
};
var app_default = app_vue_vue_type_script_setup_true_lang_default;
//#endregion
//#region node_modules/nuxt/dist/app/components/nuxt-error-page.vue
var _sfc_main$1 = {
	__name: "nuxt-error-page",
	__ssrInlineRender: true,
	props: { error: Object },
	setup(__props) {
		const _error = __props.error;
		const status = Number(_error.statusCode || 500);
		const is404 = status === 404;
		const statusText = _error.statusMessage ?? (is404 ? "Page Not Found" : "Internal Server Error");
		const description = _error.message || _error.toString();
		const stack = void 0;
		const _Error404 = defineAsyncComponent(() => import("./_nuxt/error-404-BbDjF-Xl.js"));
		const _Error = defineAsyncComponent(() => import("./_nuxt/error-500-DE5RylDA.js"));
		const ErrorTemplate = is404 ? _Error404 : _Error;
		return (_ctx, _push, _parent, _attrs) => {
			_push(ssrRenderComponent(unref(ErrorTemplate), mergeProps({
				status: unref(status),
				statusText: unref(statusText),
				statusCode: unref(status),
				statusMessage: unref(statusText),
				description: unref(description),
				stack: unref(stack)
			}, _attrs), null, _parent));
		};
	}
};
var _sfc_setup$1 = _sfc_main$1.setup;
_sfc_main$1.setup = (props, ctx) => {
	const ssrContext = useSSRContext();
	(ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("node_modules/nuxt/dist/app/components/nuxt-error-page.vue");
	return _sfc_setup$1 ? _sfc_setup$1(props, ctx) : void 0;
};
//#endregion
//#region virtual:nuxt:node_modules%2F.cache%2Fnuxt%2F.nuxt%2Fisland-renderer.mjs
var IslandRenderer = () => null;
//#endregion
//#region node_modules/nuxt/dist/app/components/nuxt-root.vue
var _sfc_main = {
	__name: "nuxt-root",
	__ssrInlineRender: true,
	setup(__props) {
		const nuxtApp = useNuxtApp();
		nuxtApp.deferHydration();
		nuxtApp.ssrContext.url;
		const SingleRenderer = false;
		provide(PageRouteSymbol, useRoute());
		nuxtApp.hooks.callHookWith((hooks) => hooks.map((hook) => hook()), "vue:setup", []);
		const error = /* @__PURE__ */ useError();
		const abortRender = error.value && !nuxtApp.ssrContext.error;
		function invokeAppErrorHandler(err, target, info) {
			const errorHandler = nuxtApp.vueApp.config.errorHandler;
			if (errorHandler && !errorHandler.__nuxt_default) try {
				errorHandler(err, target, info);
			} catch (handlerError) {
				console.error("[nuxt] Error in `app.config.errorHandler`", handlerError);
			}
		}
		onErrorCaptured((err, target, info) => {
			nuxtApp.hooks.callHook("vue:error", err, target, info)?.catch((hookError) => console.error("[nuxt] Error in `vue:error` hook", hookError));
			{
				const p = nuxtApp.runWithContext(() => showError(err));
				onServerPrefetch(() => p);
				invokeAppErrorHandler(err, target, info);
				return false;
			}
		});
		const islandContext = nuxtApp.ssrContext.islandContext;
		return (_ctx, _push, _parent, _attrs) => {
			ssrRenderSuspense(_push, {
				default: () => {
					if (unref(abortRender)) _push(`<div></div>`);
					else if (unref(error)) _push(ssrRenderComponent(unref(_sfc_main$1), { error: unref(error) }, null, _parent));
					else if (unref(islandContext)) _push(ssrRenderComponent(unref(IslandRenderer), { context: unref(islandContext) }, null, _parent));
					else if (unref(SingleRenderer)) ssrRenderVNode(_push, createVNode(resolveDynamicComponent(unref(SingleRenderer)), null, null), _parent);
					else _push(ssrRenderComponent(unref(app_default), null, null, _parent));
				},
				_: 1
			});
		};
	}
};
var _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
	const ssrContext = useSSRContext();
	(ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("node_modules/nuxt/dist/app/components/nuxt-root.vue");
	return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
//#endregion
//#region node_modules/nuxt/dist/app/entry.js
var entry = async function createNuxtAppServer(ssrContext) {
	const vueApp = createApp(_sfc_main);
	const nuxt = createNuxtApp({
		vueApp,
		ssrContext
	});
	try {
		await applyPlugins(nuxt, virtual_nuxt_node_modules_2F_cache_2Fnuxt_2F_nuxt_2Fplugins_server_default);
		await nuxt.hooks.callHook("app:created", vueApp);
	} catch (error) {
		await nuxt.hooks.callHook("app:error", error);
		nuxt.payload.error ||= createError$1(error);
	}
	if (ssrContext && (ssrContext["~renderResponse"] || ssrContext._renderResponse)) throw new Error("skipping render");
	return vueApp;
};
var entry_default = ((ssrContext) => entry(ssrContext));
//#endregion
export { useRouter as a, nuxtLinkDefaults as c, entry_default as default, resolveRouteObject as i, encodeRoutePath as n, useNuxtApp as o, navigateTo as r, useRuntimeConfig as s, useHead$1 as t };

//# sourceMappingURL=server.mjs.map